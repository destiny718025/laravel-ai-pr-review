---
quick_id: 260630-t3u
slug: codex-oauth-live-request-stream-true-str
status: complete
created: 2026-06-30
---

# Quick Task 260630-t3u: Codex OAuth Streaming Request Fix

## Task

修正 Codex OAuth live request 回傳 `Stream must be set to true` 的 backend 要求，並支援解析 streaming response。

## Plan

1. 在 `HttpOpenAICodexOAuthReviewProvider` 的 Codex `/responses` payload 加入 `stream => true`。
2. 保留既有非 streaming JSON response 解析，避免測試 fake 與可能的相容 response 被破壞。
3. 新增 SSE streaming parser，支援 `response.output_text.delta`、`response.output_text.done`、`response.completed` 與既有 response shape fallback。
4. 更新 provider、queued execution、smoke command tests，鎖定 request payload 同時包含 `store === false` 與 `stream === true`。
5. 執行 Codex 相關測試與完整測試。

## Verification

- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAICodexOAuthReviewProviderTest|CodexOAuthSmokeCommandTest|test_openai_codex_oauth_selector_runs_through_queued_execution_and_persists_validated_findings'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 composer run test`
