---
phase: 05-github-comment-publishing
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - app/Http/Controllers/ReviewDraftController.php
  - resources/views/reviews/show.blade.php
  - routes/web.php
  - tests/Feature/ReviewCommentPublishingWorkflowTest.php
  - tests/Feature/ReviewDraftWorkflowTest.php
autonomous: true
requirements:
  - PUB-01
  - PUB-05
  - PUB-06
must_haves:
  truths:
    - "Per D-01, D-02, D-03, and D-04, the review detail page offers one-click publish-all and retry-all actions only; the user cannot publish a subset of approved drafts in Phase 05."
    - "Per D-17 through D-21 and PUB-01, the Comment Drafts section renders publish/retry controls in place, shows posted and failed row state locally, exposes GitHub links for posted drafts, and shows only safe failure text for failed drafts."
    - "Per D-08, D-09, and PUB-06, posted drafts stay locally locked: they render read-only, cannot be edited, cannot have approval cancelled after publication, and Phase 05 never updates already-posted GitHub comments."
    - "Per D-13 and D-16, failed drafts stay failed until retry succeeds or fails again, and failed row UI shows only the stored safe categorized error text."
  artifacts:
    - app/Http/Controllers/ReviewDraftController.php
    - resources/views/reviews/show.blade.php
    - routes/web.php
    - tests/Feature/ReviewCommentPublishingWorkflowTest.php
    - tests/Feature/ReviewDraftWorkflowTest.php
  key_links:
    - "`ReviewDraftController` must remain thin and delegate publish/retry semantics to `ReviewCommentPublishingService`, preserving the layering constraint from PROJECT.md."
    - "`resources/views/reviews/show.blade.php` must render publish/retry controls only inside the Comment Drafts section and only when relevant statuses exist per D-17 and D-18."
    - "Workflow tests must swap `GitHubClient` through the service container so end-to-end publish UI remains fakeable per PUB-05."
  prohibitions:
    - "This plan must not add per-draft publish checkboxes, background publishing jobs, or a separate publishing page."
    - "This plan must not re-enable edit or cancel-approval controls for drafts that 05-02 has already marked `posted`, and must not add any flow for updating already-posted GitHub comments per D-09."
---

## Phase Goal

**As a** reviewer on the detail page, **I want to** publish approved drafts and retry failed drafts from the existing Comment Drafts section, **so that** I can finish the manual review workflow without leaving the run detail screen.

<objective>
Wire the Phase 05 publication foundation into the review detail workflow per D-01 through D-04, D-08, D-09, D-13, D-17 through D-21, and PUB-01, PUB-05, PUB-06.

Purpose: complete the MVP slice by exposing publish/retry controls, per-draft status display, and fakeable route coverage without weakening the explicit human-approval gate.
Output: controller actions, POST routes, Comment Drafts UI updates, and feature tests for publish/retry route behavior plus posted/failed lock behavior.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-github-comment-publishing/05-CONTEXT.md
@.planning/phases/05-github-comment-publishing/05-RESEARCH.md
@.planning/phases/05-github-comment-publishing/05-PATTERNS.md
@.planning/phases/05-github-comment-publishing/05-01-PLAN.md
@.planning/phases/05-github-comment-publishing/05-02-PLAN.md
@app/Http/Controllers/ReviewDraftController.php
@resources/views/reviews/show.blade.php
@routes/web.php
</context>

<tasks>

<task type="auto">
  <name>Task 05-03-01: Lock publish/retry detail-page behavior with RED feature tests</name>
  <files>tests/Feature/ReviewCommentPublishingWorkflowTest.php, tests/Feature/ReviewDraftWorkflowTest.php</files>
  <action>Create failing feature coverage for the final UI slice. In `ReviewCommentPublishingWorkflowTest`, bind a fake `GitHubClient` and assert the detail page shows `Publish Approved` only when approved drafts exist and `Retry Failed` only when failed drafts exist, both inside the Comment Drafts section per D-17 and D-18. Add failing POST-route assertions for `reviews.drafts.publish-approved` and `reviews.drafts.retry-failed`: each redirects back to `reviews.show`, flashes a summary from the publishing result object, publishes only through the fake client, and leaves unapproved drafts untouched per PUB-06. Assert posted rows render local posted state, `posted_at`, and the GitHub link when present per D-19 and D-20, while failed rows render only the safe categorized error text per D-21. Update `ReviewDraftWorkflowTest` with direct route tests proving `PATCH reviews.drafts.update` rejects `posted` and `failed` drafts, `POST reviews.drafts.unapprove` rejects `posted` and `failed` drafts, and no per-draft publish selector exists per D-02, D-08, D-09, and D-13.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewCommentPublishingWorkflowTest|ReviewDraftWorkflowTest'</automated>
  </verify>
  <done>The HTTP routes and Blade behavior for publish/retry and posted/failed lock behavior are described by failing tests before controller and view changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-02: Add publish/retry routes, controller actions, and Comment Drafts UI</name>
  <files>app/Http/Controllers/ReviewDraftController.php, resources/views/reviews/show.blade.php, routes/web.php, tests/Feature/ReviewCommentPublishingWorkflowTest.php, tests/Feature/ReviewDraftWorkflowTest.php</files>
  <behavior>
    - The review detail page exposes one-click publish-all and retry-all actions in the Comment Drafts section only when relevant drafts exist.
    - Controller actions call the publication service and redirect with safe summary flash messages.
    - Posted drafts render as read-only with GitHub link metadata; failed drafts render as read-only with safe error metadata.
  </behavior>
  <action>Extend `ReviewDraftController` with thin `publishApproved()` and `retryFailed()` methods that call `ReviewCommentPublishingService`, redirect back to `reviews.show`, and flash either `status` or safe `service_error_code`/`service_error_message` values without formatting GitHub or persistence logic in the controller. Add POST routes `reviews.drafts.publish-approved` and `reviews.drafts.retry-failed` adjacent to the existing draft routes in `routes/web.php`. Update `resources/views/reviews/show.blade.php` so the existing Comment Drafts section gains section-level `Publish Approved` and `Retry Failed` forms when relevant drafts exist per D-17 and D-18, but no per-draft publish selector or publish page is introduced per D-02. Render local posted/failed labels, safe error text, `posted_at`, and GitHub comment links for posted rows per D-19 through D-21. Keep edit textareas and cancel-approval controls available only for statuses that are still locally mutable, so posted drafts remain locked per D-08 and D-09 while failed drafts remain retry-only per D-13.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewCommentPublishingWorkflowTest|ReviewDraftWorkflowTest|ReviewCommentPublishingServiceTest'</automated>
  </verify>
  <done>The review detail page completes the manual GitHub publishing workflow with explicit publish/retry controls and safe per-draft status display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click on review detail page -> publish or retry controller action | Explicit human approval crosses into a GitHub-mutating workflow here. |
| persisted posted/failed draft metadata -> Blade rendering | Stored publication state becomes user-visible run detail output here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | detail-page publish actions | high | mitigate | Keep publish and retry as explicit POST actions in the Comment Drafts section only, delegate filtering to the service, and do not add subset-publish UI. |
| T-05-07 | Integrity | posted draft mutability | medium | mitigate | Hide edit and cancel-approval controls for `posted` rows and cover route-level rejection for posted/failed mutation attempts. |
| T-05-08 | Information Disclosure | failed draft UI rendering | medium | mitigate | Render only stored safe categorized error text and never show raw GitHub response, header, or token fragments. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewCommentPublishingWorkflowTest|ReviewDraftWorkflowTest|ReviewCommentPublishingServiceTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Http/Controllers/ReviewDraftController.php resources/views/reviews/show.blade.php routes/web.php tests/Feature/ReviewCommentPublishingWorkflowTest.php tests/Feature/ReviewDraftWorkflowTest.php`
</verification>

<success_criteria>
- The review detail page can publish all currently approved drafts and retry all currently failed drafts from the Comment Drafts section.
- Posted drafts show GitHub link metadata and remain locally locked from edit/unapprove controls.
- Failed drafts show only safe categorized error text, and the publish/retry route flow is fully fakeable through the GitHub client interface.
</success_criteria>

<output>
Create `.planning/phases/05-github-comment-publishing/05-03-SUMMARY.md` when done
</output>
