---
phase: 05-github-comment-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/Data/ReviewCommentPublishingResult.php
  - app/Enums/ReviewCommentDraftStatus.php
  - app/Models/ReviewCommentDraft.php
  - app/Repositories/ReviewCommentDraftRepository.php
  - app/Services/ReviewCommentPublishingService.php
  - database/migrations/2026_06_29_000000_add_publication_columns_to_review_comment_drafts_table.php
  - tests/Feature/ReviewCommentPublishingServiceTest.php
autonomous: true
requirements:
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - PUB-06
must_haves:
  truths:
    - "Per D-01, D-03, and D-04, the service exposes separate publish-approved and retry-failed workflows, and each workflow operates only on drafts in that status set."
    - "Per D-05, D-06, and D-07, the service prefers a line-level pull-request review comment when local targeting metadata is sufficient and otherwise publishes one fallback general PR comment per draft."
    - "Per D-10 through D-16 and PUB-03, PUB-04, each draft stores only safe publication outcomes: posted drafts capture GitHub comment id, HTML URL, and `posted_at`, while failed drafts capture categorized safe error code/message with no raw GitHub body, header, or token fragments."
    - "Per D-08, D-12, D-13, D-14, and PUB-06, posted drafts stay posted, failed drafts stay failed until retried, retry overwrites the prior safe failure, and later failures never roll back earlier successful publications."
  artifacts:
    - app/Data/ReviewCommentPublishingResult.php
    - app/Repositories/ReviewCommentDraftRepository.php
    - app/Services/ReviewCommentPublishingService.php
    - database/migrations/2026_06_29_000000_add_publication_columns_to_review_comment_drafts_table.php
    - tests/Feature/ReviewCommentPublishingServiceTest.php
  key_links:
    - "`ReviewCommentPublishingService` must load the review run aggregate and delegate all draft reads/writes to `ReviewCommentDraftRepository`, preserving Controller / Service / Repository layering from PROJECT.md."
    - "`ReviewCommentPublishingService` must reach GitHub only through `App\\Contracts\\GitHub\\GitHubClient`, using the publication DTOs from 05-01."
    - "`ReviewCommentDraftRepository` owns all posted/failed database mutations and clears stale failure fields on success."
  prohibitions:
    - "This plan must not add controller routes, Blade buttons, or per-row publish UI; those belong to 05-03."
    - "This plan must not store full GitHub response JSON, raw validation payloads, authorization headers, or tokens on `review_comment_drafts`."
---

## Phase Goal

**As a** reviewer, **I want to** publish approved drafts and retry failed drafts through a service layer, **so that** only explicitly approved feedback reaches GitHub and every draft keeps a safe local result.

<objective>
Build local publication persistence and service orchestration for Phase 05 per D-01 through D-16 and PUB-02 through PUB-06.

Purpose: connect approved/failed draft state to the fakeable GitHub write boundary without adding HTTP UI yet.
Output: additive draft schema fields, repository mutations, service summary result, publishing service, and RED-to-GREEN service tests.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-github-comment-publishing/05-CONTEXT.md
@.planning/phases/05-github-comment-publishing/05-RESEARCH.md
@.planning/phases/05-github-comment-publishing/05-PATTERNS.md
@.planning/phases/05-github-comment-publishing/05-01-PLAN.md
@app/Models/ReviewCommentDraft.php
@app/Repositories/ReviewCommentDraftRepository.php
@app/Services/ReviewDraftService.php
</context>

<tasks>

<task type="auto">
  <name>Task 05-02-01: Lock draft publication persistence and service semantics in RED tests</name>
  <files>tests/Feature/ReviewCommentPublishingServiceTest.php</files>
  <action>Create failing service coverage using a fake `GitHubClient` binding. Assert publish-approved touches only approved drafts and never draft/failed/posted drafts per D-01, D-02, D-04, and PUB-06. Assert retry-failed touches only failed drafts per D-03 and D-04. Assert sufficient target metadata emits the line-level client method and insufficient local target metadata falls back to one issue comment per draft per D-05 through D-07. Assert success stores only GitHub id, HTML URL, and `posted_at`; failure stores only safe error code/message; partial success keeps earlier posted drafts posted; retry overwrites prior failure data per D-10 through D-16.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=ReviewCommentPublishingServiceTest</automated>
  </verify>
  <done>The local publication service and repository contract fails under test before schema/service changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-02-02: Implement draft publication schema, repository, and service workflow</name>
  <files>app/Data/ReviewCommentPublishingResult.php, app/Enums/ReviewCommentDraftStatus.php, app/Models/ReviewCommentDraft.php, app/Repositories/ReviewCommentDraftRepository.php, app/Services/ReviewCommentPublishingService.php, database/migrations/2026_06_29_000000_add_publication_columns_to_review_comment_drafts_table.php, tests/Feature/ReviewCommentPublishingServiceTest.php</files>
  <behavior>
    - Publish-approved processes only `approved` drafts; retry-failed processes only `failed` drafts.
    - Line-level GitHub review comments are attempted only when local target metadata is sufficient; otherwise the workflow falls back to one issue comment per draft.
    - Success stores GitHub comment id, HTML URL, and `posted_at`; failure stores only safe categorized error code/message and never raw GitHub payloads.
  </behavior>
  <action>Add an additive migration that gives `review_comment_drafts` publication fields for `github_comment_id`, `github_comment_html_url`, `posted_at`, `publication_error_code`, and `publication_error_message`. Extend `ReviewCommentDraft` and `ReviewCommentDraftStatus` with casts and helpers needed for posted/failed reads. Add `ReviewCommentPublishingResult` so later controllers can flash summary counts without formatting business logic. Extend `ReviewCommentDraftRepository` with methods to load approved or failed drafts for a run, mark a draft posted, and mark a draft failed while overwriting stale failure data on retry and clearing failure fields on success. Implement `ReviewCommentPublishingService` as synchronous MVP orchestration: load the review run with pull request and repository, select drafts by mode, choose line-level versus fallback publication, call GitHub only through `GitHubClient`, persist each draft result independently, and never persist raw response bodies, request fragments, authorization headers, or tokens.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=ReviewCommentPublishingServiceTest</automated>
  </verify>
  <done>The codebase has a publish/retry service that can be fully faked and that persists only safe per-draft outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved/failed draft records -> GitHub client calls | Locally approved or retryable draft content crosses into a GitHub-mutating workflow here. |
| GitHub publication result/failure -> persisted draft publication state | External publication outcomes are reduced to safe local draft fields here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | `ReviewCommentPublishingService` status filtering | high | mitigate | Filter drafts by service mode and cover approved-only / failed-only behavior with service tests. |
| T-05-04 | Information Disclosure | publication fields on `review_comment_drafts` | high | mitigate | Persist only safe categorized error code/message and `github_comment_id`, `github_comment_html_url`, `posted_at` on success. |
| T-05-05 | Integrity | batch publish / retry persistence | medium | mitigate | Persist each draft independently so partial success never rolls back earlier posted drafts, and overwrite prior failure state on retry. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=ReviewCommentPublishingServiceTest`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Data/ReviewCommentPublishingResult.php app/Enums/ReviewCommentDraftStatus.php app/Models/ReviewCommentDraft.php app/Repositories/ReviewCommentDraftRepository.php app/Services/ReviewCommentPublishingService.php tests/Feature/ReviewCommentPublishingServiceTest.php`
</verification>

<success_criteria>
- The service can publish approved drafts and retry failed drafts entirely through `App\Contracts\GitHub\GitHubClient`.
- Draft publication persists safe posted/failed state per row without storing raw GitHub payloads.
- Service tests cover line-level publication, fallback issue comments, partial success, retry overwrite, and safe failure persistence.
</success_criteria>

<output>
Create `.planning/phases/05-github-comment-publishing/05-02-SUMMARY.md` when done
</output>
