---
phase: 05-github-comment-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Contracts/GitHub/GitHubClient.php
  - app/Data/GitHub/GitHubCommentPublicationResult.php
  - app/Data/GitHub/GitHubCommentPublicationTarget.php
  - app/Services/GitHub/GitHubFailureMapper.php
  - app/Services/GitHub/HttpGitHubClient.php
  - tests/Fixtures/GitHub/pull-request-issue-comment.json
  - tests/Fixtures/GitHub/pull-request-review-comment.json
  - tests/Unit/GitHub/GitHubFailureMapperTest.php
  - tests/Unit/GitHub/HttpGitHubClientPublicationTest.php
autonomous: true
requirements:
  - PUB-02
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "Per D-05, D-06, and D-07, the GitHub client boundary supports both line-level pull-request review comments and one-comment-per-draft fallback issue comments."
    - "Per D-10, D-11, D-15, D-16, PUB-02, and PUB-04, GitHub publication responses are reduced to safe result DTOs and failures are mapped to categorized safe messages without raw response bodies, headers, or tokens."
    - "Per PUB-05, all GitHub publication behavior can be tested through Laravel HTTP fakes or a fake `GitHubClient` binding without live GitHub calls."
  artifacts:
    - app/Contracts/GitHub/GitHubClient.php
    - app/Data/GitHub/GitHubCommentPublicationResult.php
    - app/Data/GitHub/GitHubCommentPublicationTarget.php
    - app/Services/GitHub/GitHubFailureMapper.php
    - app/Services/GitHub/HttpGitHubClient.php
    - tests/Unit/GitHub/GitHubFailureMapperTest.php
    - tests/Unit/GitHub/HttpGitHubClientPublicationTest.php
  key_links:
    - "`GitHubClient` remains the only app-level boundary for GitHub publication, so later services never call `Http::` directly."
    - "`HttpGitHubClient` must reuse its existing `request()` method so base URL, API version, accept header, and token handling stay centralized."
    - "`GitHubFailureMapper` must preserve existing fetch-safe categories while adding publication-safe target validation handling."
  prohibitions:
    - "This plan must not add draft database columns, publish service orchestration, routes, controller methods, or Blade UI; those belong to later Phase 05 plans."
    - "This plan must not store full GitHub response JSON or expose raw GitHub response fragments in test expectations."
---

## Phase Goal

**As a** developer implementing GitHub publication, **I want to** add a safe fakeable GitHub write boundary, **so that** draft publishing can be built without coupling services or tests to live GitHub HTTP calls.

<objective>
Build the GitHub publication client layer for Phase 05 per D-05 through D-07, D-10, D-11, D-15, D-16, and PUB-02, PUB-04, PUB-05.

Purpose: establish safe outbound publication DTOs and HTTP client methods before local draft persistence or UI wiring.
Output: GitHub publication DTOs, interface methods, HTTP implementation, fixture responses, and safe failure mapper coverage.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-github-comment-publishing/05-CONTEXT.md
@.planning/phases/05-github-comment-publishing/05-RESEARCH.md
@.planning/phases/05-github-comment-publishing/05-PATTERNS.md
@app/Contracts/GitHub/GitHubClient.php
@app/Services/GitHub/GitHubFailureMapper.php
@app/Services/GitHub/HttpGitHubClient.php
</context>

<tasks>

<task type="auto">
  <name>Task 05-01-01: Lock GitHub publication endpoints and safe failure mapping in RED tests</name>
  <files>tests/Fixtures/GitHub/pull-request-issue-comment.json, tests/Fixtures/GitHub/pull-request-review-comment.json, tests/Unit/GitHub/GitHubFailureMapperTest.php, tests/Unit/GitHub/HttpGitHubClientPublicationTest.php</files>
  <action>Create failing HTTP-client and mapper coverage. In `HttpGitHubClientPublicationTest`, use `Http::fake()` to assert `POST /repos/{owner}/{repo}/pulls/{pull_number}/comments` receives `body`, `commit_id`, `path`, `line`, and `side: RIGHT`, and `POST /repos/{owner}/{repo}/issues/{pull_number}/comments` receives only the fallback body. Both tests should parse only `id`, `html_url`, and a posted timestamp from fixtures. Extend `GitHubFailureMapperTest` with publication-safe `target_invalid`, `auth_failed`, `rate_limited`, and malformed-response assertions that do not expose raw response fragments per D-15 and D-16.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='HttpGitHubClientPublicationTest|GitHubFailureMapperTest'</automated>
  </verify>
  <done>The GitHub write API contract and safe failure categories fail under test before client changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-01-02: Implement GitHub publication DTOs and HTTP client write methods</name>
  <files>app/Contracts/GitHub/GitHubClient.php, app/Data/GitHub/GitHubCommentPublicationResult.php, app/Data/GitHub/GitHubCommentPublicationTarget.php, app/Services/GitHub/GitHubFailureMapper.php, app/Services/GitHub/HttpGitHubClient.php, tests/Unit/GitHub/GitHubFailureMapperTest.php, tests/Unit/GitHub/HttpGitHubClientPublicationTest.php</files>
  <behavior>
    - GitHub line-level publication uses the pull-request review comments endpoint through `GitHubClient`.
    - Fallback publication uses the issues comments endpoint through `GitHubClient`.
    - Success and failure payloads remain safe and minimal.
  </behavior>
  <action>Add readonly publication DTOs for the target and result values needed by later service work. Extend `GitHubClient` with explicit publication methods for pull-request review comments and fallback issue comments. Implement those methods in `HttpGitHubClient` using the existing `request()` helper, parse only `id`, `html_url`, and `created_at` or equivalent timestamp into `GitHubCommentPublicationResult`, and throw malformed-response errors for unexpected shapes. Extend `GitHubFailureMapper` to map validation or unprocessable publication responses into a safe `target_invalid` category while preserving existing GitHub fetch failure behavior.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='HttpGitHubClientPublicationTest|GitHubFailureMapperTest'</automated>
  </verify>
  <done>The GitHub client interface and HTTP implementation support safe fakeable publication without live network calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local publication target DTO -> GitHub HTTP request | Draft-derived data becomes an outbound GitHub mutation request here. |
| GitHub HTTP response -> result/failure DTO | External response data is reduced to a safe local shape here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `GitHubCommentPublicationTarget` request construction | high | mitigate | Cover request payload shape for line-level and fallback comments with HTTP-fake tests. |
| T-05-02 | Information Disclosure | `GitHubFailureMapper` and result parsing | high | mitigate | Persistible outputs contain only safe categories plus `id`, `html_url`, and posted timestamp; tests reject raw response/header/token fragments. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='HttpGitHubClientPublicationTest|GitHubFailureMapperTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Contracts/GitHub/GitHubClient.php app/Data/GitHub/GitHubCommentPublicationResult.php app/Data/GitHub/GitHubCommentPublicationTarget.php app/Services/GitHub/GitHubFailureMapper.php app/Services/GitHub/HttpGitHubClient.php tests/Unit/GitHub/HttpGitHubClientPublicationTest.php tests/Unit/GitHub/GitHubFailureMapperTest.php`
</verification>

<success_criteria>
- GitHub publication is available only through `App\Contracts\GitHub\GitHubClient`.
- HTTP client tests cover both line-level PR review comments and fallback issue comments without live GitHub requests.
- Publication failures map to safe categorized messages and do not expose raw GitHub payloads.
</success_criteria>

<output>
Create `.planning/phases/05-github-comment-publishing/05-01-SUMMARY.md` when done
</output>
