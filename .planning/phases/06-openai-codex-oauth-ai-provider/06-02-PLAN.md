---
phase: 06-openai-codex-oauth-ai-provider
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/Services/AI/AIReviewFailureMapper.php
  - app/Services/AI/HttpOpenAICodexOAuthReviewProvider.php
  - tests/Unit/AI/AIReviewFailureMapperTest.php
  - tests/Unit/AI/OpenAICodexOAuthReviewProviderTest.php
  - tests/Unit/AI/OpenAIReviewProviderTest.php
autonomous: true
requirements:
  - AI-03
  - AI-04
  - AI-08
  - EXEC-04
  - EXEC-05
must_haves:
  truths:
    - "Per D-15, D-16, D-21, D-22, and D-23, `openai_codex_oauth` resolves to its own HTTP provider class, reuses the existing `AIReviewRequest`, sends the full review context to the Codex `/responses` transport, and extracts raw review JSON text from the resolved Responses-style output contract for the existing validator."
    - "Per D-17, D-18, D-19, and D-20, missing auth, malformed auth, 401 or 403, 429, transport failure, malformed response, unsupported response shape, and invalid provider output all map to categorized safe failures with no API-key fallback and no token or raw-body leakage."
    - "Per D-25, D-26, and D-27, unit tests fake both auth credentials and Codex backend HTTP; no real Codex, ChatGPT, OpenAI, or GitHub call is allowed."
  artifacts:
    - app/Services/AI/AIReviewFailureMapper.php
    - app/Services/AI/HttpOpenAICodexOAuthReviewProvider.php
    - tests/Unit/AI/AIReviewFailureMapperTest.php
    - tests/Unit/AI/OpenAICodexOAuthReviewProviderTest.php
    - tests/Unit/AI/OpenAIReviewProviderTest.php
  key_links:
    - "`HttpOpenAICodexOAuthReviewProvider` must depend on `CodexAuthCacheReader` and return only raw JSON text extracted from `output[].content[]` `output_text`/`text` parts or the `output_text` fallback, leaving decoding and schema validation to the existing execution pipeline."
    - "`AIReviewFailureMapper` must classify typed auth-cache failures and HTTP status failures before its generic JSON, schema, and runtime branches."
    - "Codex provider tests must assert that Codex mode does not retry against the OpenAI API-key base URL when Codex auth or backend calls fail."
  prohibitions:
    - "This plan must not add automatic fallback to `OPENAI_API_KEY`, provider-specific branches in `ReviewExecutionService`, or any persistence of backend response bodies."
---

## Phase Goal

**As a** reviewer running AI analysis through a Codex subscription session, **I want to** call the Codex backend through the existing provider contract, **so that** successful reviews return structured JSON and failures stay explicit, categorized, and safe.

<objective>
Implement the Codex OAuth HTTP provider and extend safe failure mapping per D-15 through D-27, AI-03, AI-04, AI-08, EXEC-04, and EXEC-05.

Purpose: add the third concrete provider behind `AIReviewProvider` without changing the queued execution contract or allowing silent fallback to the API-key route.
Output: `HttpOpenAICodexOAuthReviewProvider`, expanded `AIReviewFailureMapper`, selector-resolution coverage for the Codex path, and offline HTTP/auth unit tests.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-CONTEXT.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-RESEARCH.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-PATTERNS.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-VALIDATION.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-01-PLAN.md
@app/Services/AI/HttpOpenAIReviewProvider.php
@app/Services/AI/AIReviewFailureMapper.php
@tests/Unit/AI/AIReviewFailureMapperTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 06-02-01: Add Wave 0 RED tests for Codex transport semantics and categorized safe failures</name>
  <files>tests/Unit/AI/AIReviewFailureMapperTest.php, tests/Unit/AI/OpenAICodexOAuthReviewProviderTest.php, tests/Unit/AI/OpenAIReviewProviderTest.php</files>
  <action>Create failing unit coverage for the Codex transport and failure matrix. Extend `OpenAIReviewProviderTest` so D-15 is enforced by asserting `services.ai.provider=openai_codex_oauth` resolves the Codex provider distinctly. Add `OpenAICodexOAuthReviewProviderTest` with `Http::preventStrayRequests()` plus fake auth credentials to assert the provider posts the existing `AIReviewRequest` fields to `${services.codex.base_url}/responses`, sends bearer auth from the cache, sends `ChatGPT-Account-ID` when the credential DTO has an account id, extracts structured review JSON text from a Responses object shaped as `output[].content[]` entries whose `type` is `output_text` or `text`, supports top-level `output_text` as the compatibility fallback, rejects unsupported success shapes per D-16 and D-21 through D-24, and never retries against the API-key base URL per D-17 and D-18. Extend `AIReviewFailureMapperTest` with missing-auth, malformed-auth, 401 or 403, 429, transport, malformed-response, and unsupported-shape cases per D-19 and D-20.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAIReviewProviderTest|OpenAICodexOAuthReviewProviderTest|AIReviewFailureMapperTest'</automated>
  </verify>
  <done>The Codex transport contract and categorized failure expectations are locked into failing unit coverage before provider code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-02-02: Implement the Codex OAuth provider and extend failure mapping without fallback</name>
  <files>app/Services/AI/AIReviewFailureMapper.php, app/Services/AI/HttpOpenAICodexOAuthReviewProvider.php, tests/Unit/AI/AIReviewFailureMapperTest.php, tests/Unit/AI/OpenAICodexOAuthReviewProviderTest.php, tests/Unit/AI/OpenAIReviewProviderTest.php</files>
  <behavior>
    - `openai_codex_oauth` sends the current `AIReviewRequest` through the Codex backend and returns raw JSON text for the existing validator.
    - Unauthorized, rate-limited, malformed, unsupported, and auth-cache failures map to stable sanitized messages.
    - Codex mode never switches to the API-key transport automatically.
  </behavior>
  <action>Implement `HttpOpenAICodexOAuthReviewProvider` using `CodexAuthCacheReader` plus Laravel HTTP, default base URL `https://chatgpt.com/backend-api/codex`, config-driven timeout, and POST `/responses` request construction that preserves instructions, repository identity, PR number, source URL, head SHA, title, and changed files per D-16 and D-21 through D-23. Send `Authorization: Bearer <accessToken>` and add `ChatGPT-Account-ID` when the credential DTO carries an account id; missing account id must not crash or fallback. Extract text from the resolved Responses contract by scanning `output[]` message items and their `content[]` entries for `type` values of `output_text` or `text`, then fall back to top-level `output_text`; throw safe unsupported-response errors when no review JSON text exists per D-24. Expand `AIReviewFailureMapper` so typed auth-cache failures, request 401 or 403, 429, connection failures, malformed success bodies, unsupported response shapes, JSON decode errors, and schema failures each map to sanitized stable codes and messages per D-19 and D-20. Do not add automatic API-key fallback, do not persist backend bodies, and do not leak auth/header/token fragments in exceptions or mapped messages per D-17 through D-20.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAIReviewProviderTest|OpenAICodexOAuthReviewProviderTest|AIReviewFailureMapperTest'</automated>
  </verify>
  <done>The codebase has a tested Codex OAuth provider path behind `AIReviewProvider`, and all new Codex failures reduce to safe categorized summaries only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime Codex credentials -> outbound HTTP request | Imported local session data becomes a network authentication header here. |
| Codex backend response/error -> provider output or mapped failure | Untrusted upstream bodies and statuses are condensed into application-safe outputs here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | `HttpOpenAICodexOAuthReviewProvider` request/response handling | high | mitigate | Use reader-provided credentials only in-memory, extract only review text, and reject raw backend body persistence or display. |
| T-06-05 | Repudiation | provider fallback behavior | high | mitigate | Assert and implement no automatic switch from Codex OAuth to the API-key route when Codex auth or backend calls fail. |
| T-06-06 | Tampering | unofficial response-shape parsing | medium | mitigate | Support only the resolved Responses-style `output[].content[]` text parts plus `output_text` fallback, and map unsupported success shapes to safe invalid-output failures. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAIReviewProviderTest|OpenAICodexOAuthReviewProviderTest|AIReviewFailureMapperTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Services/AI/AIReviewFailureMapper.php app/Services/AI/HttpOpenAICodexOAuthReviewProvider.php tests/Unit/AI/AIReviewFailureMapperTest.php tests/Unit/AI/OpenAICodexOAuthReviewProviderTest.php tests/Unit/AI/OpenAIReviewProviderTest.php`
</verification>

<success_criteria>
- `openai_codex_oauth` resolves to its own provider implementation and reuses the existing `AIReviewRequest` contract unchanged.
- Codex success paths return raw JSON text compatible with the current validator from the resolved Responses-style output contract.
- Codex auth and backend failures map to safe categorized summaries with no API-key fallback and no secret leakage.
</success_criteria>

<output>
Create `.planning/phases/06-openai-codex-oauth-ai-provider/06-02-SUMMARY.md` when done
</output>
