---
phase: 06-openai-codex-oauth-ai-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Data/AI/CodexAuthCredentials.php
  - app/Exceptions/AI/CodexAuthException.php
  - app/Providers/AppServiceProvider.php
  - app/Services/AI/CodexAuthCacheReader.php
  - config/services.php
  - tests/Unit/AI/CodexAuthCacheReaderTest.php
  - tests/Unit/AI/OpenAIReviewProviderTest.php
autonomous: true
requirements:
  - ARCH-01
  - ARCH-05
  - AI-03
  - EXEC-05
must_haves:
  truths:
    - "Per D-11, D-12, D-13, and D-14, AI provider selection is explicit and config-driven, `fake` remains the default deterministic path, `openai_api_key` stays distinct, and unsupported selectors fail closed instead of silently falling through."
    - "Per D-01, D-02, D-03, D-08, D-09, and D-10, a dedicated runtime Codex auth-cache reader resolves override -> `CODEX_HOME/auth.json` -> `~/.codex/auth.json`, exposes only the fields the provider needs, and safe-fails when the cache is missing, unreadable, malformed, or lacks an access token."
    - "Per D-04 and D-05, Phase 06 reuses Codex CLI login state only and does not introduce browser callback OAuth or device-code UI inside Laravel."
    - "Per D-06, D-07, EXEC-05, and the schema gate, Phase 06 adds no migration or token persistence; imported Codex credentials stay out of the database, logs, storage paths, and failure text."
  artifacts:
    - app/Data/AI/CodexAuthCredentials.php
    - app/Exceptions/AI/CodexAuthException.php
    - app/Providers/AppServiceProvider.php
    - app/Services/AI/CodexAuthCacheReader.php
    - config/services.php
    - tests/Unit/AI/CodexAuthCacheReaderTest.php
    - tests/Unit/AI/OpenAIReviewProviderTest.php
  key_links:
    - "`AppServiceProvider` remains the only container binding point for `AIReviewProvider`, preserving Controller / Service / Repository layering per ARCH-01."
    - "`CodexAuthCacheReader` is the only filesystem seam for local Codex auth state and must consume config rather than direct `env()` lookups."
    - "Unit tests must use temp files or config overrides only; they must not read a real `~/.codex/auth.json` or make live HTTP calls."
  prohibitions:
    - "This plan must not add database tables, token persistence, OAuth UI, device-code flow, localhost callback flow, or refresh-token rotation per D-03 through D-10."
    - "This plan must not treat the old boolean `services.openai.enabled` flag as the provider selector once `AI_PROVIDER` exists."
---

## Phase Goal

**As a** reviewer configuring AI execution, **I want to** select providers explicitly and reuse local Codex CLI auth safely, **so that** queued reviews can adopt Codex OAuth without storing tokens or hiding which route is active.

<objective>
Establish the Phase 06 provider-selection and auth-cache foundation per D-01 through D-14, ARCH-01, ARCH-05, AI-03, and EXEC-05.

Purpose: replace the ambiguous boolean OpenAI toggle with explicit provider selection and a fakeable runtime auth reader before any Codex backend HTTP work begins.
Output: selector config, container binding, Codex auth DTO/exception/reader classes, and RED-to-GREEN unit coverage for selector and auth-cache behavior.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-CONTEXT.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-RESEARCH.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-PATTERNS.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-VALIDATION.md
@app/Providers/AppServiceProvider.php
@config/services.php
@tests/Unit/AI/OpenAIReviewProviderTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 06-01-01: Add Wave 0 RED tests for explicit selector behavior and safe Codex auth-cache reading</name>
  <files>tests/Unit/AI/CodexAuthCacheReaderTest.php, tests/Unit/AI/OpenAIReviewProviderTest.php</files>
  <action>Create failing unit coverage for the foundation decisions. In `OpenAIReviewProviderTest`, lock D-11, D-12, D-13, and D-14 by asserting `services.ai.provider=fake` resolves `FakeAIReviewProvider`, `services.ai.provider=openai_api_key` resolves `HttpOpenAIReviewProvider`, and unsupported selectors throw instead of silently falling through. Add `CodexAuthCacheReaderTest` using temp JSON files and config overrides to prove path precedence per D-01 and D-02, safe failure on missing unreadable malformed cache or missing `access_token` per D-09, and zero token or raw file-body leakage per D-06, D-07, D-08, D-10, and EXEC-05. Keep every test offline and never read a real user auth file.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAIReviewProviderTest|CodexAuthCacheReaderTest'</automated>
  </verify>
  <done>The selector contract and auth-cache failure matrix are captured in failing unit coverage before config or reader code changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-01-02: Implement explicit provider config, safe auth reader, and fail-closed binding</name>
  <files>app/Data/AI/CodexAuthCredentials.php, app/Exceptions/AI/CodexAuthException.php, app/Providers/AppServiceProvider.php, app/Services/AI/CodexAuthCacheReader.php, config/services.php, tests/Unit/AI/CodexAuthCacheReaderTest.php, tests/Unit/AI/OpenAIReviewProviderTest.php</files>
  <behavior>
    - Provider selection is keyed by explicit config rather than the old boolean OpenAI flag.
    - The auth reader discovers only the configured or conventional Codex auth file locations and returns a minimal runtime credential DTO.
    - Missing or malformed auth state fails safely without leaking token material or raw file bodies.
  </behavior>
  <action>Add `services.ai.provider` backed by `AI_PROVIDER` plus `services.codex` config for auth-path override, Codex home, fallback home path, base URL, and timeout so application services stay config-only per D-02 and EXEC-05. Create readonly `CodexAuthCredentials` and a typed `CodexAuthException` carrying safe reason metadata only. Implement `CodexAuthCacheReader` as the only filesystem seam: resolve override -> `CODEX_HOME/auth.json` -> `~/.codex/auth.json`, parse only `access_token`, optional `account_id`, optional `auth_mode`, and optional `last_refresh`, and throw safe typed failures without echoing file contents or token fragments per D-01 through D-10. Keep login ownership outside Laravel by reusing Codex CLI state only and by not adding browser callback or device-code OAuth flows per D-03, D-04, and D-05. Update `AppServiceProvider` so `AIReviewProvider` selection is an explicit match on `fake`, `openai_api_key`, and `openai_codex_oauth`, with `fake` remaining default and unknown selectors throwing `InvalidArgumentException` instead of falling back.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAIReviewProviderTest|CodexAuthCacheReaderTest'</automated>
  </verify>
  <done>The codebase has explicit provider selection plus a fakeable runtime auth reader, and no token persistence or schema work has been introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config selector -> container binding | Environment-derived provider choice becomes executable application behavior here. |
| local auth cache file -> runtime credential DTO | Untrusted filesystem content is reduced to a minimal in-memory credential shape here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `CodexAuthCacheReader` | high | mitigate | Parse only required fields, throw safe typed failures, and prove tests never surface raw auth JSON or token fragments. |
| T-06-02 | Spoofing | `AIReviewProvider` selector binding | medium | mitigate | Replace the boolean OpenAI toggle with explicit `AI_PROVIDER` matching and fail closed on unsupported values. |
| T-06-03 | Tampering | auth-path resolution | medium | mitigate | Restrict discovery to explicit override, `CODEX_HOME/auth.json`, and `~/.codex/auth.json`, all covered by unit tests. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='OpenAIReviewProviderTest|CodexAuthCacheReaderTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Data/AI/CodexAuthCredentials.php app/Exceptions/AI/CodexAuthException.php app/Providers/AppServiceProvider.php app/Services/AI/CodexAuthCacheReader.php config/services.php tests/Unit/AI/CodexAuthCacheReaderTest.php tests/Unit/AI/OpenAIReviewProviderTest.php`
</verification>

<success_criteria>
- `AI_PROVIDER` is the only authoritative selector for `fake`, `openai_api_key`, and the future `openai_codex_oauth` branch.
- `CodexAuthCacheReader` can be fully tested with temp files and config overrides, and it never persists or reveals imported token material.
- No migration or token-storage work exists in this phase foundation.
</success_criteria>

<output>
Create `.planning/phases/06-openai-codex-oauth-ai-provider/06-01-SUMMARY.md` when done
</output>
