---
phase: 06-openai-codex-oauth-ai-provider
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - app/Services/ReviewExecutionService.php
  - tests/Feature/QueuedReviewExecutionTest.php
  - tests/Feature/QueuedReviewFailureTest.php
autonomous: true
requirements:
  - ARCH-03
  - AI-04
  - AI-08
  - EXEC-04
  - EXEC-05
must_haves:
  truths:
    - "Per D-21, D-22, and D-23, queued review execution remains provider-agnostic: `ReviewExecutionService` still builds one `AIReviewRequest`, calls `AIReviewProvider::review()`, decodes the returned JSON text, and validates findings without adding Codex-specific branches."
    - "Per D-09, D-19, D-20, AI-08, and EXEC-04, queued failures persist only safe summary text for missing auth, malformed auth, unauthorized, rate-limited, transport, malformed-response, and unsupported-shape cases."
    - "Per D-25, D-26, D-27, and EXEC-05, feature tests fake auth reads and HTTP calls with the resolved Responses-style success shape, require no external credentials, and prove no bearer access refresh or id token, raw auth cache body, raw backend body, `ChatGPT-Account-ID`, or auth header fragment is persisted."
  artifacts:
    - app/Services/ReviewExecutionService.php
    - tests/Feature/QueuedReviewExecutionTest.php
    - tests/Feature/QueuedReviewFailureTest.php
  key_links:
    - "`ReviewExecutionService` remains the single decode/validate/fail orchestration point, so new Codex behavior must flow through the existing validator and failure mapper."
    - "Feature tests should exercise the real selector plus provider path with fake reader and fake HTTP rather than bypassing the container."
    - "No schema or repository expansion is allowed; safe failure persistence must continue through existing review-run writes only."
  prohibitions:
    - "This plan must not add provider-specific branches to controllers or jobs, and it must not add any migration because the schema gate forbids token persistence for Phase 06."
---

## Phase Goal

**As a** reviewer running queued analysis end to end, **I want to** use the Codex OAuth provider through the existing execution pipeline, **so that** successful runs still persist validated findings and failure runs still store only safe summaries.

<objective>
Harden queued execution integration and complete the Phase 06 offline verification gate per D-09, D-19 through D-27, ARCH-03, AI-04, AI-08, EXEC-04, and EXEC-05.

Purpose: prove the new provider path fits the existing queued workflow unchanged at the contract boundary and that persisted review-run failures remain secret-free.
Output: feature coverage for success and failure through the Codex selector, any minimal `ReviewExecutionService` adjustments needed to keep the flow provider-agnostic, and a passing full Docker test gate.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-CONTEXT.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-RESEARCH.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-PATTERNS.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-VALIDATION.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-01-PLAN.md
@.planning/phases/06-openai-codex-oauth-ai-provider/06-02-PLAN.md
@app/Services/ReviewExecutionService.php
@tests/Feature/QueuedReviewExecutionTest.php
@tests/Feature/QueuedReviewFailureTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 06-03-01: Add Wave 0 RED feature coverage for provider-agnostic queued execution and secret-free persistence</name>
  <files>tests/Feature/QueuedReviewExecutionTest.php, tests/Feature/QueuedReviewFailureTest.php</files>
  <action>Create failing feature coverage that exercises the new selector through the existing queue path. In `QueuedReviewExecutionTest`, set `services.ai.provider=openai_codex_oauth`, fake the auth reader or configured auth path plus a Codex HTTP success body using the resolved Responses-style shape `output[].content[]` with `type=output_text` and JSON text, run the existing queue job, and assert validated findings persist through the unchanged `AIReviewProvider` contract per D-21 through D-23 and AI-04. In `QueuedReviewFailureTest`, add missing-auth, malformed-auth, 401 or 403, 429, transport, malformed-response, and unsupported-shape scenarios per D-09, D-19, and D-26, and assert `review_runs.safe_error_message` never contains bearer access refresh or id token fragments, raw auth cache JSON, raw backend JSON, `Authorization`, or `ChatGPT-Account-ID` per D-20 and EXEC-05. Keep all coverage offline with fake file reads and `Http::fake()` per D-25 and D-27.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='QueuedReviewExecutionTest|QueuedReviewFailureTest'</automated>
  </verify>
  <done>The queued Codex success and failure behaviors are captured in failing feature coverage before any execution-path adjustment is made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-03-02: Preserve provider-agnostic execution and complete the full offline phase gate</name>
  <files>app/Services/ReviewExecutionService.php, tests/Feature/QueuedReviewExecutionTest.php, tests/Feature/QueuedReviewFailureTest.php</files>
  <behavior>
    - Queued execution continues to consume only `AIReviewProvider::review(AIReviewRequest)` plus the existing decode and validation boundary.
    - Codex-specific auth and backend failures persist only mapped safe summaries, including failures from missing account id or rejected `ChatGPT-Account-ID` requests.
    - The full test suite runs inside Docker with no live Codex, OpenAI, ChatGPT, or GitHub access.
  </behavior>
  <action>Make only the minimal execution-path changes required so `ReviewExecutionService` continues to treat every provider identically: build `AIReviewRequest`, call `AIReviewProvider::review()`, decode, validate, and route every Codex auth or backend exception through `AIReviewFailureMapper` before persisting the safe summary per D-21 through D-24, AI-08, and EXEC-04. Preserve repository-owned failure persistence, preserve successful findings persistence, and keep Codex-specific decisions out of controllers, jobs, and database schema per ARCH-03, D-06, D-07, and the schema gate. End this plan by running the Phase 06 gate `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 composer run test`; no real Codex, OpenAI, ChatGPT, or GitHub calls or credentials are permitted.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 composer run test</automated>
  </verify>
  <done>The Codex OAuth path works through the existing queued review contract end to end, and persisted failures remain secret-free under the full offline test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider output/failure -> queued execution service | Provider-specific behavior becomes review-run persistence state here. |
| persisted review-run safe error -> feature assertions and UI | Stored failure summaries become user-visible status text here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Information Disclosure | `review_runs.safe_error_message` | high | mitigate | Extend feature assertions to reject token fragments, raw auth cache bodies, raw backend bodies, `ChatGPT-Account-ID`, and auth headers across all Codex failure modes. |
| T-06-08 | Tampering | `ReviewExecutionService` provider boundary | medium | mitigate | Keep one provider-agnostic decode/validate/fail path and forbid Codex-specific branching in the execution service. |
| T-06-09 | Denial of Service | queued failure regression | medium | mitigate | Cover missing-auth, 401 or 403, 429, transport, malformed-response, and unsupported-shape scenarios in feature tests and run the full Docker suite. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='QueuedReviewExecutionTest|QueuedReviewFailureTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 composer run test`
</verification>

<success_criteria>
- The existing queued execution flow can run `openai_codex_oauth` without changing the `AIReviewProvider` contract or adding provider-specific persistence branches.
- Failed Codex runs persist only safe categorized summaries and never store or expose imported credential or backend payload fragments.
- The full Docker test suite passes with fake auth reads and fake external HTTP only.
</success_criteria>

<output>
Create `.planning/phases/06-openai-codex-oauth-ai-provider/06-03-SUMMARY.md` when done
</output>
