---
phase: 03-queued-ai-review-and-structured-findings
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/Contracts/AI/AIReviewProvider.php
  - app/Data/AI/AIReviewRequest.php
  - app/Providers/AppServiceProvider.php
  - app/Services/AI/FakeAIReviewProvider.php
  - app/Services/AI/ReviewInstructionBuilder.php
  - tests/Fixtures/AI/fake-review-invalid.json
  - tests/Fixtures/AI/fake-review-valid.json
  - tests/Unit/AI/FakeAIReviewProviderTest.php
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-06
  - AI-07
must_haves:
  truths:
    - "Per D-05 and D-08, queued review execution depends on an `AIReviewProvider` interface whose default Phase 3 binding stays offline and deterministic."
    - "Per D-13 and the locked revision vocabulary decision, `ReviewInstructionBuilder::buildDefault()` explicitly prioritizes `bug` and `security` findings first, allows `performance`, `maintainability`, and `style` only when useful, and constrains severity language to `critical`, `high`, `medium`, and `low`."
    - "Per ARCH-01 through ARCH-04, controllers and workflow services consume the contract plus DTOs only; container binding remains in `AppServiceProvider` rather than inline vendor selection."
  artifacts:
    - app/Contracts/AI/AIReviewProvider.php
    - app/Data/AI/AIReviewRequest.php
    - app/Providers/AppServiceProvider.php
    - app/Services/AI/FakeAIReviewProvider.php
    - app/Services/AI/ReviewInstructionBuilder.php
    - tests/Fixtures/AI/fake-review-invalid.json
    - tests/Fixtures/AI/fake-review-valid.json
    - tests/Unit/AI/FakeAIReviewProviderTest.php
  key_links:
    - "`AppServiceProvider` binds `AIReviewProvider` to `FakeAIReviewProvider` for the default execution path established in Phase 3."
    - "`AIReviewRequest` is the only payload type workflow services pass into provider code; no controller or repository reaches into concrete provider classes."
    - "`ReviewInstructionBuilder` must encode the shared severity/category vocabulary so later validator, fixtures, and UI work use the same tokens."
---

## Phase Goal

**As a** reviewer running queued AI analysis, **I want to** execute through a deterministic provider seam, **so that** later validation and persistence work has a stable contract instead of vendor-specific behavior.

<objective>
Create the provider contract, request DTO, fake provider, and default instruction builder that make Phase 3 fake-provider-first per D-05, D-08, and D-13.

Purpose: establish the provider boundary and prompt baseline before validation, failure mapping, or optional concrete-adapter work layers on top.
Output: `AIReviewProvider`, `AIReviewRequest`, `FakeAIReviewProvider`, `ReviewInstructionBuilder`, fixture payloads, default container binding, and passing offline unit coverage.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-CONTEXT.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-PATTERNS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-VALIDATION.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-01-PLAN.md
@app/Providers/AppServiceProvider.php
@app/Contracts/GitHub/GitHubClient.php
@app/Services/GitHub/HttpGitHubClient.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED tests for the fake-first provider contract and default instructions</name>
  <files>tests/Unit/AI/FakeAIReviewProviderTest.php</files>
  <read_first>.planning/phases/03-queued-ai-review-and-structured-findings/03-VALIDATION.md, .planning/phases/03-queued-ai-review-and-structured-findings/03-PATTERNS.md, tests/Unit/GitHub/GitHubFailureMapperTest.php, app/Contracts/GitHub/GitHubClient.php</read_first>
  <action>Create `tests/Unit/AI/FakeAIReviewProviderTest.php` with failing coverage for D-05, D-08, and D-13. Assert `app(AIReviewProvider::class)` resolves through the container, `FakeAIReviewProvider` returns deterministic JSON backed by `tests/Fixtures/AI/fake-review-valid.json`, and the provider can be pointed at `tests/Fixtures/AI/fake-review-invalid.json` for later failure tests. Assert `ReviewInstructionBuilder::buildDefault()` contains the shared vocabulary the phase will enforce: default emphasis on `bug` and `security`, optional `performance`, `maintainability`, and `style` findings only when useful and not noisy, and severity labels limited to `critical`, `high`, `medium`, and `low`.</action>
  <acceptance_criteria>`FakeAIReviewProviderTest` fails because the AI review contract, request DTO, fake provider, fixtures, and instruction builder do not exist yet. The failing test establishes that Phase 3 remains fake-provider-first and does not require live OpenAI access.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=FakeAIReviewProviderTest</automated>
  </verify>
  <done>The provider contract, fixture seam, and default instruction vocabulary are captured in failing offline tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the provider contract, request DTO, fake provider, and default binding</name>
  <files>app/Contracts/AI/AIReviewProvider.php, app/Data/AI/AIReviewRequest.php, app/Providers/AppServiceProvider.php, app/Services/AI/FakeAIReviewProvider.php, app/Services/AI/ReviewInstructionBuilder.php, tests/Fixtures/AI/fake-review-invalid.json, tests/Fixtures/AI/fake-review-valid.json, tests/Unit/AI/FakeAIReviewProviderTest.php</files>
  <read_first>app/Providers/AppServiceProvider.php, app/Contracts/GitHub/GitHubClient.php, app/Services/GitHub/HttpGitHubClient.php, .planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md</read_first>
  <behavior>
    - `AIReviewProvider::review(AIReviewRequest $request): string` returns raw JSON so validation remains centralized in later plans.
    - `FakeAIReviewProvider` is the default Phase 3 binding and never makes network calls.
    - `ReviewInstructionBuilder::buildDefault(): string` carries the shared severity/category vocabulary and the bug/security-first guidance required by D-13.
  </behavior>
  <action>Create `App\Contracts\AI\AIReviewProvider` and readonly DTO `App\Data\AI\AIReviewRequest` carrying repository full name, pull request number, source URL, head SHA, GitHub title, and changed-file snapshots. Implement `App\Services\AI\ReviewInstructionBuilder::buildDefault(): string` so it tells the provider to prioritize `bug` and `security`, include `performance` or `maintainability` when warranted, allow `style` only when useful and not noisy, and express finding severity as `critical`, `high`, `medium`, or `low`. Implement `App\Services\AI\FakeAIReviewProvider` with deterministic fixture-backed JSON, defaulting to `tests/Fixtures/AI/fake-review-valid.json` but switchable to the invalid fixture inside tests. Update `AppServiceProvider::register()` so services resolve `AIReviewProvider::class` through the fake provider by default. Keep controllers thin, keep provider selection in the container, and do not add validation, failure mapping, findings persistence, comment-draft tables, or publication behavior in this plan.</action>
  <acceptance_criteria>`FakeAIReviewProviderTest` passes, provider resolution stays interface-first, the default instruction builder encodes the locked vocabulary, and no live provider credentials or network access are required.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=FakeAIReviewProviderTest</automated>
  </verify>
  <done>The queued review workflow now has a deterministic provider seam and prompt baseline that later plans can safely build on.</done>
</task>

</tasks>

## Artifacts this phase produces

- `app/Contracts/AI/AIReviewProvider.php` — `AIReviewProvider`
- `app/Data/AI/AIReviewRequest.php` — `AIReviewRequest`
- `app/Providers/AppServiceProvider.php` — default `AIReviewProvider` binding
- `app/Services/AI/FakeAIReviewProvider.php` — fake deterministic provider
- `app/Services/AI/ReviewInstructionBuilder.php` — default provider instructions
- `tests/Fixtures/AI/fake-review-valid.json`
- `tests/Fixtures/AI/fake-review-invalid.json`
- `tests/Unit/AI/FakeAIReviewProviderTest.php`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution service → AI provider contract | Workflow code crosses from trusted orchestration into provider output generation here. |
| fixture JSON → test assertions | Fixture payloads simulate untrusted provider output and must remain deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | provider selection | medium | mitigate | Bind `AIReviewProvider` in `AppServiceProvider` so controllers and services depend on the interface only. |
| T-03-05 | Denial of Service | provider transport | medium | mitigate | Keep `FakeAIReviewProvider` as the default Phase 3 path so execution and tests do not depend on external network access. |
| T-03-06 | Tampering | review instructions vocabulary | medium | mitigate | Encode the shared severity/category vocabulary in `ReviewInstructionBuilder` so later validator and UI work consume the same tokens. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs are introduced; this plan uses existing Laravel components only. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=FakeAIReviewProviderTest`
</verification>

<success_criteria>
- The app resolves AI review behavior through an interface-first, fake-provider-first seam.
- Default instructions explicitly encode the Phase 3 finding vocabulary and priorities.
- No controller, repository, or queue code depends directly on a concrete provider class.
</success_criteria>

<output>
Create `.planning/phases/03-queued-ai-review-and-structured-findings/03-02-SUMMARY.md` when done
</output>
