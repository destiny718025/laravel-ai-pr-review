---
phase: 03-queued-ai-review-and-structured-findings
plan: 05
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - app/Models/ReviewFinding.php
  - app/Models/ReviewRun.php
  - app/Repositories/ReviewFindingRepository.php
  - app/Repositories/ReviewRunRepository.php
  - app/Services/ReviewExecutionService.php
  - database/factories/ReviewFindingFactory.php
  - database/migrations/2026_06_28_000000_create_review_findings_table.php
  - resources/views/reviews/show.blade.php
  - tests/Feature/QueuedReviewExecutionTest.php
  - tests/Feature/QueuedReviewFailureTest.php
autonomous: true
requirements:
  - EXEC-03
  - EXEC-04
  - AI-05
must_haves:
  truths:
    - "Per D-09, D-10, D-11, and AI-05, successful review execution persists structured findings with `severity`, `category`, `file_path`, optional `line_reference`, `title`, `rationale`, and `suggested_comment_text`."
    - "Per D-14 through D-18 and EXEC-04, failed execution stores only safe summaries, allows manual retry, and replaces prior findings after a successful retry."
    - "Per D-12 and the roadmap boundary, the review detail page shows findings only; it does not create comment-draft records, approval state, or GitHub publication controls."
  artifacts:
    - app/Models/ReviewFinding.php
    - app/Models/ReviewRun.php
    - app/Repositories/ReviewFindingRepository.php
    - app/Repositories/ReviewRunRepository.php
    - app/Services/ReviewExecutionService.php
    - database/factories/ReviewFindingFactory.php
    - database/migrations/2026_06_28_000000_create_review_findings_table.php
    - resources/views/reviews/show.blade.php
    - tests/Feature/QueuedReviewExecutionTest.php
    - tests/Feature/QueuedReviewFailureTest.php
  key_links:
    - "`ReviewExecutionService::execute()` must hand only validated finding DTOs to repository persistence and mark the run completed in the same transaction."
    - "`ReviewFindingRepository::replaceForReviewRun()` must replace the entire finding set for a run so retries cannot mix stale and fresh results."
    - "`resources/views/reviews/show.blade.php` must render persisted findings without introducing draft-edit, approval, or publication controls."
---

## Phase Goal

**As a** reviewer returning to a finished run, **I want to** see persisted structured findings and safe retry behavior, **so that** the detail page reflects the latest validated AI execution result for that PR snapshot.

<objective>
Finish Phase 3 by persisting validated findings, rendering them on the review detail page, and preserving safe retry/failure behavior per D-09 through D-18.

Purpose: turn the validated provider output from earlier plans into durable review findings while preserving the phase boundary that stops before draft records and GitHub publication.
Output: `review_findings` persistence, repository replacement logic, retry-safe execution writes, findings UI, and passing feature coverage for success and failure flows.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-CONTEXT.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-PATTERNS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-VALIDATION.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-03-PLAN.md
@app/Models/ReviewRun.php
@app/Repositories/ReviewRunRepository.php
@resources/views/reviews/show.blade.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for findings persistence, safe failure display, and retry replacement</name>
  <files>tests/Feature/QueuedReviewExecutionTest.php, tests/Feature/QueuedReviewFailureTest.php</files>
  <read_first>.planning/phases/03-queued-ai-review-and-structured-findings/03-VALIDATION.md, .planning/phases/03-queued-ai-review-and-structured-findings/03-PATTERNS.md, tests/Feature/GitHubPullRequestIngestionFailureTest.php, tests/Feature/ReviewRunHistoryAndDetailTest.php</read_first>
  <action>Extend `tests/Feature/QueuedReviewExecutionTest.php` with failing happy-path coverage that runs `POST /reviews/{reviewRun}/run`, executes the queued job, and asserts `review_findings` rows are written with `severity`, `category`, `file_path`, `line_reference`, `title`, `rationale`, and `suggested_comment_text` per D-10 and AI-05. Extend `tests/Feature/QueuedReviewFailureTest.php` with failing coverage for D-14 through D-18: invalid provider output or runtime errors mark the run `failed` with safe summaries only, and a subsequent successful retry deletes stale finding rows, clears `safe_error_message`, clears `failed_at`, and writes the fresh finding set. Assert the detail page shows findings but no draft-edit, approval, or publish controls per D-12.</action>
  <acceptance_criteria>The feature tests fail because the `review_findings` table, `ReviewFinding` model, replacement repository, retry cleanup writes, and findings UI do not exist yet. The tests explicitly prevent Phase 3 from introducing comment-draft rows or GitHub publication controls.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='QueuedReviewExecutionTest|QueuedReviewFailureTest'</automated>
  </verify>
  <done>Persistence, retry replacement, and detail-page rendering requirements are locked into failing feature coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the findings schema, model, repository, and relation wiring</name>
  <files>database/migrations/2026_06_28_000000_create_review_findings_table.php, database/factories/ReviewFindingFactory.php, app/Models/ReviewFinding.php, app/Models/ReviewRun.php, app/Repositories/ReviewFindingRepository.php, app/Repositories/ReviewRunRepository.php, tests/Feature/QueuedReviewExecutionTest.php</files>
  <read_first>app/Models/ReviewRun.php, app/Models/ReviewRunFile.php, app/Repositories/ReviewRunRepository.php, database/factories/UserFactory.php, .planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md</read_first>
  <behavior>
    - `review_findings` rows belong to `review_runs` and store the full AI-05 field set.
    - `ReviewRun` exposes `findings(): HasMany`.
    - `ReviewFindingRepository::replaceForReviewRun(ReviewRun $reviewRun, array $findings): void` replaces the full finding set transactionally for one run.
  </behavior>
  <action>Create migration `database/migrations/2026_06_28_000000_create_review_findings_table.php` with foreign key `review_run_id`, string columns `severity`, `category`, `file_path`, nullable string `line_reference`, string `title`, text `rationale`, text `suggested_comment_text`, and timestamps. Add `App\Models\ReviewFinding` with fillable fields and `reviewRun()` relation, extend `App\Models\ReviewRun` with `findings()` relation, and add `database/factories/ReviewFindingFactory.php` for feature tests. Create `App\Repositories\ReviewFindingRepository::replaceForReviewRun(ReviewRun $reviewRun, array $findings): void` that deletes prior rows and inserts the validated collection inside a transaction per D-18. Update `ReviewRunRepository::findWithPullRequestRepositoryOrFail()` to eager-load `findings` for detail-page rendering. Keep persistence in repository classes, not controllers or execution jobs, to preserve the Controller / Service / Repository boundary.</action>
  <acceptance_criteria>`php artisan migrate:fresh --env=testing` passes inside the container, happy-path execution tests can assert against `review_findings`, and retry replacement has a dedicated repository seam instead of ad hoc writes from the service layer.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan migrate:fresh --env=testing</automated>
  </verify>
  <done>The application has durable storage and repository seams for structured review findings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist validated findings in execution, harden retry-safe failures, and render findings</name>
  <files>app/Repositories/ReviewRunRepository.php, app/Services/ReviewExecutionService.php, resources/views/reviews/show.blade.php, tests/Feature/QueuedReviewExecutionTest.php, tests/Feature/QueuedReviewFailureTest.php</files>
  <read_first>app/Services/ReviewExecutionService.php, app/Repositories/ReviewRunRepository.php, resources/views/reviews/show.blade.php, .planning/phases/03-queued-ai-review-and-structured-findings/03-CONTEXT.md</read_first>
  <behavior>
    - Successful execution persists the validated finding set and only then marks the run `completed`.
    - Failed execution writes only safe summary text and leaves malformed findings absent.
    - Successful retry replaces the prior findings for the same `review_run_id` and refreshes the detail page with the latest result.
  </behavior>
  <action>Update `ReviewExecutionService::execute()` so validated finding DTOs are passed to `ReviewFindingRepository::replaceForReviewRun()` and then `ReviewRunRepository::markCompleted(ReviewRun $reviewRun): ReviewRun` inside one transaction satisfying EXEC-03 and D-17/D-18. Add or update `ReviewRunRepository::markExecutionFailed(ReviewRun $reviewRun, string $safeErrorMessage): ReviewRun` so provider, decode, validation, and runtime failures store only safe summaries per D-14 and D-15. Extend `resources/views/reviews/show.blade.php` with a `Structured Findings` section that renders severity, category, file path, optional line reference, title, rationale, and suggested comment text from persisted findings, but do not add draft edit forms, approval buttons, status columns, comment-draft records, or GitHub publication controls per D-12. Ensure retry via the existing `Run AI Review` action is permitted after failure per D-16.</action>
  <acceptance_criteria>`QueuedReviewExecutionTest` and `QueuedReviewFailureTest` pass, persisted findings appear on the detail page, safe errors never contain raw provider payload fragments or secrets, and a successful retry leaves only the latest validated finding set for that run.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='QueuedReviewExecutionTest|QueuedReviewFailureTest'</automated>
  </verify>
  <done>Phase 3 ends with persisted structured findings, retry replacement semantics, and findings rendering on the existing review detail page.</done>
</task>

</tasks>

## Artifacts this phase produces

- `app/Models/ReviewFinding.php` — `ReviewFinding`
- `app/Models/ReviewRun.php` — findings relation
- `app/Repositories/ReviewFindingRepository.php` — findings replacement persistence
- `app/Repositories/ReviewRunRepository.php` — eager loading and failure/completion transitions
- `app/Services/ReviewExecutionService.php` — validated findings persistence orchestration
- `database/factories/ReviewFindingFactory.php`
- `database/migrations/2026_06_28_000000_create_review_findings_table.php`
- `resources/views/reviews/show.blade.php` — findings display
- `tests/Feature/QueuedReviewExecutionTest.php`
- `tests/Feature/QueuedReviewFailureTest.php`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated findings → database | Structured findings cross from transient execution state into durable storage here. |
| persisted failures/findings → review detail page | Safe error summaries and persisted findings become user-visible here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering | `review_findings` replacement writes | high | mitigate | Replace the full finding set transactionally through `ReviewFindingRepository::replaceForReviewRun()` so retries cannot mix stale and fresh findings. |
| T-03-14 | Information Disclosure | `review_runs.safe_error_message` and findings UI | high | mitigate | Persist only whitelisted safe summaries and render only persisted finding fields; never display raw provider payloads or credentials. |
| T-03-15 | Elevation of Privilege | Phase 3 detail page actions | medium | mitigate | Keep the detail page limited to run/retry plus findings display; do not add draft approval or GitHub publication controls reserved for later phases. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs are introduced; persistence uses existing Laravel migrations, models, and repositories. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan migrate:fresh --env=testing`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='QueuedReviewExecutionTest|QueuedReviewFailureTest'`
</verification>

<success_criteria>
- Successful queued execution persists validated findings with the full AI-05 field set.
- Invalid output and runtime failures leave malformed findings absent and expose only safe summaries.
- Retrying a failed run replaces the prior findings set and refreshes the detail page to the latest result.
</success_criteria>

<output>
Create `.planning/phases/03-queued-ai-review-and-structured-findings/03-05-SUMMARY.md` when done
</output>
