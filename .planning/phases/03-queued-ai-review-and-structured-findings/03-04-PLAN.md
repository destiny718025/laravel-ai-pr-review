---
phase: 03-queued-ai-review-and-structured-findings
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - app/Providers/AppServiceProvider.php
  - app/Services/AI/HttpOpenAIReviewProvider.php
  - config/services.php
  - tests/Unit/AI/OpenAIReviewProviderTest.php
autonomous: true
requirements:
  - AI-03
  - EXEC-05
must_haves:
  truths:
    - "Per D-06, D-07, and the locked revision decision, Phase 3 includes an opt-in `HttpOpenAIReviewProvider` behind the AI provider interface, but the default execution path remains fake-provider-first."
    - "Per EXEC-05, `services.openai` is reserved in config and the adapter reads secrets from config only; no live OpenAI call is required for verification."
    - "Per ARCH-01 through ARCH-04, provider selection remains a container concern and never becomes controller or repository branching."
  artifacts:
    - app/Providers/AppServiceProvider.php
    - app/Services/AI/HttpOpenAIReviewProvider.php
    - config/services.php
    - tests/Unit/AI/OpenAIReviewProviderTest.php
  key_links:
    - "`AppServiceProvider` must choose between `FakeAIReviewProvider` and `HttpOpenAIReviewProvider` based on `services.openai.enabled`."
    - "`HttpOpenAIReviewProvider` must consume the same `AIReviewRequest` contract and return raw JSON compatible with later validation logic."
    - "`config/services.php` is the only approved source of `OPENAI_*` environment lookups."
---

## Phase Goal

**As a** reviewer preparing for a future live provider, **I want to** keep a concrete adapter available behind the same interface, **so that** Phase 3 stays fake-first while reserving a tested opt-in OpenAI seam.

<objective>
Add the optional `HttpOpenAIReviewProvider` seam, config-only secret handling, and HTTP-fakeable unit coverage behind `AIReviewProvider`, per D-06, D-07, D-08, and the locked revision decisions.

Purpose: make future live-provider work incremental instead of architectural while preserving the fake-first Phase 3 default path.
Output: `HttpOpenAIReviewProvider`, `services.openai.*` config, config-gated container binding, and offline unit tests.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-CONTEXT.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-PATTERNS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-02-PLAN.md
@app/Providers/AppServiceProvider.php
@config/services.php
@app/Services/GitHub/HttpGitHubClient.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED unit coverage for the opt-in OpenAI adapter seam</name>
  <files>tests/Unit/AI/OpenAIReviewProviderTest.php</files>
  <read_first>.planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md, .planning/phases/03-queued-ai-review-and-structured-findings/03-PATTERNS.md, tests/Unit/AI/FakeAIReviewProviderTest.php, app/Services/GitHub/HttpGitHubClient.php</read_first>
  <action>Create `tests/Unit/AI/OpenAIReviewProviderTest.php` with failing coverage for D-06 through D-08, AI-03, and EXEC-05. Add one test that sets `services.openai.enabled=true`, `services.openai.base_url`, `services.openai.api_key`, `services.openai.model`, and `services.openai.timeout`, then asserts `app(AIReviewProvider::class)` resolves to `HttpOpenAIReviewProvider`. Add a second test using `Http::preventStrayRequests()` plus `Http::fake()` to assert `HttpOpenAIReviewProvider::review(AIReviewRequest $request)` sends a structured-output request and returns raw JSON text without persisting or logging secrets. Add a final test proving `services.openai.enabled=false` keeps `FakeAIReviewProvider` as the default binding so Phase 3 execution remains fake-provider-first.</action>
  <acceptance_criteria>`OpenAIReviewProviderTest` fails because the config-gated adapter and selection logic do not exist yet. The tests define an offline-only contract and must not require a live OpenAI key or network access.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=OpenAIReviewProviderTest</automated>
  </verify>
  <done>Concrete adapter expectations are captured in failing unit coverage without changing the default execution path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the config-gated OpenAI adapter without changing the fake-first default</name>
  <files>app/Providers/AppServiceProvider.php, app/Services/AI/HttpOpenAIReviewProvider.php, config/services.php, tests/Unit/AI/OpenAIReviewProviderTest.php</files>
  <read_first>app/Providers/AppServiceProvider.php, config/services.php, app/Services/GitHub/HttpGitHubClient.php, .planning/phases/03-queued-ai-review-and-structured-findings/03-RESEARCH.md</read_first>
  <behavior>
    - `AIReviewProvider` resolves to `HttpOpenAIReviewProvider` only when `services.openai.enabled` is true.
    - `HttpOpenAIReviewProvider::review(AIReviewRequest $request): string` uses Laravel HTTP, reads config only, and returns raw JSON text.
    - Tests pass entirely with `Http::fake()` and `Http::preventStrayRequests()`.
  </behavior>
  <action>Add `services.openai` entries in `config/services.php` for `enabled`, `base_url`, `api_key`, `model`, and `timeout`, sourcing only `OPENAI_*` environment variables per D-06 and EXEC-05. Implement `App\Services\AI\HttpOpenAIReviewProvider` using Laravel HTTP with config-driven base URL, timeout, and bearer-token handling, returning the raw JSON string consumed later by validation logic. Update `AppServiceProvider::register()` to bind `AIReviewProvider::class` through a closure that returns `HttpOpenAIReviewProvider` only when `services.openai.enabled` is true and otherwise keeps `FakeAIReviewProvider` as the Phase 3 default path. Do not add live smoke tests, controller branching, persisted provider payload storage, or any requirement for live OpenAI access in this plan.</action>
  <acceptance_criteria>`OpenAIReviewProviderTest` passes, `config/services.php` remains the only source of OpenAI env references, and the application has an opt-in adapter seam without changing Phase 3's fake-first execution default.</acceptance_criteria>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=OpenAIReviewProviderTest</automated>
  </verify>
  <done>The codebase now has a tested optional OpenAI adapter seam behind the same provider interface.</done>
</task>

</tasks>

## Artifacts this phase produces

- `app/Providers/AppServiceProvider.php` — config-gated provider selection
- `app/Services/AI/HttpOpenAIReviewProvider.php` — optional concrete provider
- `config/services.php` — reserved `services.openai.*` config
- `tests/Unit/AI/OpenAIReviewProviderTest.php`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app config → OpenAI adapter | Secrets cross from environment/config into adapter requests here. |
| OpenAI adapter → external HTTP endpoint | A future live endpoint is untrusted and must remain fully fakeable in tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Information Disclosure | `services.openai` secrets | high | mitigate | Keep `OPENAI_*` lookups in `config/services.php` only and avoid persisting or echoing credential values. |
| T-03-11 | Spoofing | provider selection | medium | mitigate | Gate concrete-adapter resolution in `AppServiceProvider` so workflow code continues to depend on `AIReviewProvider` only. |
| T-03-12 | Denial of Service | live provider dependency in tests | medium | mitigate | Require `Http::fake()` and `Http::preventStrayRequests()` in `OpenAIReviewProviderTest`, keeping verification offline. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs are introduced; the adapter uses existing Laravel HTTP tooling. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter=OpenAIReviewProviderTest`
</verification>

<success_criteria>
- A concrete `HttpOpenAIReviewProvider` exists behind `AIReviewProvider`.
- OpenAI configuration is reserved in `config/services.php` and remains optional.
- Phase 3 verification stays offline and fake-provider-first even after the adapter exists.
</success_criteria>

<output>
Create `.planning/phases/03-queued-ai-review-and-structured-findings/03-04-SUMMARY.md` when done
</output>
