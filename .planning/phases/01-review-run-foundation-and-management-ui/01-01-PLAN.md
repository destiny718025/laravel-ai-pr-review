---
phase: 01-review-run-foundation-and-management-ui
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_27_000001_create_repositories_table.php
  - database/migrations/2026_06_27_000002_create_pull_requests_table.php
  - database/migrations/2026_06_27_000003_create_review_runs_table.php
  - app/Enums/ReviewRunStatus.php
  - app/Models/GitHubRepository.php
  - app/Models/PullRequest.php
  - app/Models/ReviewRun.php
  - tests/Feature/ReviewRunSchemaTest.php
autonomous: true
requirements:
  - RUN-04
  - ARCH-04
must_haves:
  truths:
    - "D-01/D-03: Review run status vocabulary is exactly pending, queued, running, completed, failed, and cancelled."
    - "D-07/D-08/D-09/D-10: repositories, pull_requests, and review_runs are separate persisted tables with matching Eloquent models."
    - "RUN-04: The schema can persist review run status, source URL through pull request identity, repository owner/name, pull request number, safe error message, and timestamps."
    - "ARCH-04: Domain database reads and writes are prepared for repository classes; this plan does not create controllers, services, URL parsers, routes, or UI."
  artifacts:
    - "database/migrations/2026_06_27_000001_create_repositories_table.php"
    - "database/migrations/2026_06_27_000002_create_pull_requests_table.php"
    - "database/migrations/2026_06_27_000003_create_review_runs_table.php"
    - "app/Enums/ReviewRunStatus.php"
    - "app/Models/GitHubRepository.php"
    - "app/Models/PullRequest.php"
    - "app/Models/ReviewRun.php"
    - "tests/Feature/ReviewRunSchemaTest.php"
  key_links:
    - "GitHubRepository has many PullRequest records; PullRequest belongs to GitHubRepository and has many ReviewRun records; ReviewRun belongs to PullRequest."
    - "ReviewRun casts status to ReviewRunStatus and lifecycle timestamps to datetimes."
    - "safe_error_message exists for later safe failure display, but this plan does not create failed runs from invalid PR URLs."
---

## Phase Goal

**As a** local/private Laravel AI PR Review user, **I want to** submit a GitHub pull request URL and see a persisted review run in the management UI, **so that** I can prove the review workflow foundation works before adding GitHub fetching and AI analysis.

<objective>
Create only the Phase 1 schema, Eloquent models, relationships, and review run status foundation.

Purpose: Establish the durable review run data model before parser, service, repository, controller, or UI work depends on it.
Output: Migrations, models, status enum, and focused schema/model tests.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-CONTEXT.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-RESEARCH.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-PATTERNS.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-VALIDATION.md
@AGENTS.md
@app/Models/User.php
@database/migrations/0001_01_01_000000_create_users_table.php
@database/migrations/0001_01_01_000001_create_cache_table.php
@database/migrations/0001_01_01_000002_create_jobs_table.php
@phpunit.xml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove schema, relationships, and status vocabulary with failing tests</name>
  <read_first>.planning/phases/01-review-run-foundation-and-management-ui/01-CONTEXT.md, .planning/phases/01-review-run-foundation-and-management-ui/01-RESEARCH.md, .planning/phases/01-review-run-foundation-and-management-ui/01-VALIDATION.md, tests/Feature/ExampleTest.php, phpunit.xml</read_first>
  <files>tests/Feature/ReviewRunSchemaTest.php</files>
  <action>Create tests/Feature/ReviewRunSchemaTest.php with RefreshDatabase. Assert ReviewRunStatus cases/values are exactly pending, queued, running, completed, failed, and cancelled per D-01 and D-03. Assert repositories, pull_requests, and review_runs tables can persist owner, name, lower-case full_name, pull request number, source_url, status, safe_error_message, lifecycle timestamps, and regular timestamps per D-07 through D-10 and RUN-04. Assert relationships work from GitHubRepository to PullRequest to ReviewRun and back. Assert ReviewRun status casts to ReviewRunStatus and timestamp fields cast to date objects. These tests should initially fail because the migrations, enum, and models do not exist.</action>
  <verify>
    <automated>php artisan test --testsuite=Feature --filter=ReviewRunSchemaTest</automated>
  </verify>
  <acceptance_criteria>The failing tests encode D-01, D-03, D-07, D-08, D-09, D-10, RUN-04, and ARCH-04 without introducing parser, service, repository, route, controller, or UI expectations.</acceptance_criteria>
  <done>The schema/model tests exist and describe the required persistence foundation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement migrations, models, and status enum</name>
  <read_first>app/Models/User.php, database/migrations/0001_01_01_000000_create_users_table.php, database/migrations/0001_01_01_000001_create_cache_table.php, database/migrations/0001_01_01_000002_create_jobs_table.php, .planning/phases/01-review-run-foundation-and-management-ui/01-PATTERNS.md, .planning/codebase/CONVENTIONS.md</read_first>
  <files>database/migrations/2026_06_27_000001_create_repositories_table.php, database/migrations/2026_06_27_000002_create_pull_requests_table.php, database/migrations/2026_06_27_000003_create_review_runs_table.php, app/Enums/ReviewRunStatus.php, app/Models/GitHubRepository.php, app/Models/PullRequest.php, app/Models/ReviewRun.php</files>
  <action>Create anonymous Laravel migrations for repositories, pull_requests, and review_runs. The repositories table stores id, owner, name, full_name, timestamps, and a unique index on full_name; full_name is the normalized lower-case owner/name per D-08. The pull_requests table stores id, repository_id constrained to repositories, number as an unsigned integer, source_url, timestamps, and a unique index on repository_id plus number per D-09. The review_runs table stores id, pull_request_id constrained to pull_requests, status string, nullable safe_error_message, nullable queued_at, started_at, completed_at, failed_at, cancelled_at timestamps, and timestamps per D-10. Create ReviewRunStatus as a backed enum with exactly pending, queued, running, completed, failed, and cancelled per D-01 and D-03. Create GitHubRepository, PullRequest, and ReviewRun models following User.php style, including Fillable attributes, relationships, explicit table name for GitHubRepository using repositories, and casts for ReviewRunStatus and lifecycle timestamps. Do not add URL parsing, service DTOs, repositories, routes, controllers, views, queue dispatch, GitHub API clients, AI provider code, findings, drafts, or auth in this plan.</action>
  <verify>
    <automated>php artisan test --testsuite=Feature --filter=ReviewRunSchemaTest</automated>
  </verify>
  <acceptance_criteria>The schema/model tests pass; status vocabulary is exact; migrations run on SQLite :memory:; relationships and casts support later repository/service work.</acceptance_criteria>
  <done>The schema, models, and enum are implemented and pass the focused schema tests.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the foundation remains narrow</name>
  <read_first>.planning/phases/01-review-run-foundation-and-management-ui/01-VALIDATION.md, composer.json, phpunit.xml</read_first>
  <files>tests/Feature/ReviewRunSchemaTest.php, database/migrations/2026_06_27_000001_create_repositories_table.php, database/migrations/2026_06_27_000002_create_pull_requests_table.php, database/migrations/2026_06_27_000003_create_review_runs_table.php, app/Enums/ReviewRunStatus.php, app/Models/GitHubRepository.php, app/Models/PullRequest.php, app/Models/ReviewRun.php</files>
  <action>Run focused and full verification. Fix any schema/model failures while preserving this plan's scope boundary: no PR URL parser, no DTOs, no service, no repository classes, no routes, no controller, no Blade UI, no GitHub API client, no AI provider interface, no queue dispatch, no findings, no drafts, no auth middleware, and no destructive review-run actions.</action>
  <verify>
    <automated>php artisan test --testsuite=Feature</automated>
    <automated>composer run test</automated>
  </verify>
  <acceptance_criteria>Feature suite and full Composer test command pass with only the schema/model/status foundation added.</acceptance_criteria>
  <done>The foundation passes verification and stays below the Phase 1 Wave 1 boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

- `App\Enums\ReviewRunStatus` in `app/Enums/ReviewRunStatus.php`
- `App\Models\GitHubRepository` in `app/Models/GitHubRepository.php`
- `App\Models\PullRequest` in `app/Models/PullRequest.php`
- `App\Models\ReviewRun` in `app/Models/ReviewRun.php`
- Database tables `repositories`, `pull_requests`, and `review_runs`
- Schema/model tests in `tests/Feature/ReviewRunSchemaTest.php`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration schema to application models | Persisted domain records become Eloquent state available to later services and views. |
| Status strings to enum casts | Stored status values cross into typed application code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | ReviewRunStatus | medium | mitigate | Lock exact status vocabulary in enum and tests so unsupported states do not silently appear. |
| T-01-01-02 | Information Disclosure | review_runs.safe_error_message | medium | mitigate | Store a dedicated safe summary field for later UI use; do not store raw exceptions or provider payloads in this plan. |
| T-01-01-03 | Denial of Service | repositories and pull_requests uniqueness | low | mitigate | Add uniqueness constraints for normalized repository identity and PR identity to avoid duplicate identity rows. |
</threat_model>

<verification>
Run `php artisan test --testsuite=Feature --filter=ReviewRunSchemaTest`, then `php artisan test --testsuite=Feature`, then `composer run test`. Confirm migrations run in SQLite :memory: and no external network calls or later-phase integration code are introduced.
</verification>

<success_criteria>
Wave 1 is complete when the schema, models, relationships, casts, and exact status enum support persisted review run identity and lifecycle fields while leaving parser/service/repository/controller/UI work to dependent plans.
</success_criteria>

<output>
Create `.planning/phases/01-review-run-foundation-and-management-ui/01-01-SUMMARY.md` when done.
</output>
