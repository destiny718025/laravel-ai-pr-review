---
phase: 01-review-run-foundation-and-management-ui
plan: 01-04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - app/Http/Controllers/ReviewController.php
  - app/Repositories/ReviewRunRepository.php
  - resources/views/reviews/index.blade.php
  - resources/views/reviews/show.blade.php
  - resources/views/components/review-status.blade.php
  - tests/Feature/ReviewRunHistoryAndDetailTest.php
autonomous: true
requirements:
  - RUN-01
  - RUN-05
  - RUN-06
  - RUN-07
  - ARCH-01
  - ARCH-02
  - ARCH-04
must_haves:
  truths:
    - "D-05/RUN-05: /reviews lists recent review runs newest first with status and basic PR identity."
    - "D-06/RUN-06: /reviews/{id} is a detail shell for run metadata, status, timestamps, source URL, and safe errors only."
    - "RUN-07: failed review runs display safe_error_message, fallback safe copy when no safe summary exists, and the required next-step sentence: Review the safe error summary, then create a new run after fixing the source issue."
    - "D-01/D-03: pending, queued, running, completed, failed, and cancelled all have stable visual treatment even when Phase 1 naturally creates only pending."
    - "ARCH-02/ARCH-04: Controller loads detail/history through ReviewRunRepository and Blade views do not perform database queries."
  artifacts:
    - "Completed resources/views/reviews/show.blade.php detail shell from Plan 01-03"
    - "tests/Feature/ReviewRunHistoryAndDetailTest.php"
    - "Completed dashboard history rendering in resources/views/reviews/index.blade.php"
    - "Completed status component in resources/views/components/review-status.blade.php"
  key_links:
    - "ReviewController@index uses ReviewRunRepository::recentWithPullRequestRepository() or an equivalent eager-loaded read method."
    - "ReviewController@show uses ReviewRunRepository::findWithPullRequestRepositoryOrFail() or an equivalent eager-loaded detail method."
    - "History rows and detail pages link through route reviews.show using the ReviewRun id."
---

## Phase Goal

**As a** local/private Laravel AI PR Review user, **I want to** submit a GitHub pull request URL and see a persisted review run in the management UI, **so that** I can prove the review workflow foundation works before adding GitHub fetching and AI analysis.

<objective>
Complete the review history and detail shell for persisted review runs.

Purpose: Let the user scan created review runs, open a specific run, and see status/failure information safely without implying GitHub fetching or AI analysis exists.
Output: Finished history list, detail view, safe failure rendering, status presentation for all reserved statuses, and feature tests.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-CONTEXT.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-RESEARCH.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-PATTERNS.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-UI-SPEC.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-VALIDATION.md
@resources/css/app.css
@phpunit.xml
@.planning/phases/01-review-run-foundation-and-management-ui/01-01-SUMMARY.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-02-SUMMARY.md
@.planning/phases/01-review-run-foundation-and-management-ui/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add history, detail, and failure-state feature tests</name>
  <read_first>.planning/phases/01-review-run-foundation-and-management-ui/01-UI-SPEC.md, .planning/phases/01-review-run-foundation-and-management-ui/01-VALIDATION.md, .planning/phases/01-review-run-foundation-and-management-ui/01-01-SUMMARY.md, .planning/phases/01-review-run-foundation-and-management-ui/01-02-SUMMARY.md, .planning/phases/01-review-run-foundation-and-management-ui/01-03-SUMMARY.md</read_first>
  <files>tests/Feature/ReviewRunHistoryAndDetailTest.php</files>
  <action>Create tests/Feature/ReviewRunHistoryAndDetailTest.php with RefreshDatabase. Use ReviewRunService for normal pending runs where practical, and direct model/repository setup for reserved status and failed status display states. Assert /reviews remains accessible without authentication per RUN-01 and lists newest review runs first with visible status, repository full_name, PR number, source_url, created timestamp, and a detail link using copy View review run per D-05 and RUN-05. Assert /reviews/{id} displays Back to review runs, Review Run #{id}, status pill, repository owner/name, PR #{number}, clickable source_url, created timestamp, updated timestamp when present, and pending summary "This review run is ready for the next processing step." per D-06 and RUN-06. Assert failed review runs display heading Review run failed, only safe_error_message, fallback "The run failed, but no safe error summary was recorded." when safe_error_message is null, and the required next-step sentence "Review the safe error summary, then create a new run after fixing the source issue." per RUN-07 and 01-UI-SPEC. Assert queued, running, completed, failed, and cancelled render title-case labels without layout-breaking missing branches per D-01 and D-03. These tests should initially fail until the history/detail UI is completed.</action>
  <verify>
    <automated>php artisan test --testsuite=Feature --filter=ReviewRunHistoryAndDetailTest</automated>
  </verify>
  <acceptance_criteria>Feature tests cover RUN-01, RUN-05, RUN-06, RUN-07, D-01, D-03, D-05, and D-06, including the required failed-detail next-step sentence, with no external GitHub, AI, queue, findings, draft, auth, or publishing assumptions.</acceptance_criteria>
  <done>The history, detail, reserved status, safe failure, and failed-detail next-step tests exist and fail until the UI is completed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish repository-backed history and detail views</name>
  <read_first>app/Http/Controllers/ReviewController.php, app/Repositories/ReviewRunRepository.php, resources/views/reviews/index.blade.php, resources/views/reviews/show.blade.php, resources/views/components/review-status.blade.php, .planning/phases/01-review-run-foundation-and-management-ui/01-UI-SPEC.md</read_first>
  <files>app/Http/Controllers/ReviewController.php, app/Repositories/ReviewRunRepository.php, resources/views/reviews/index.blade.php, resources/views/reviews/show.blade.php, resources/views/components/review-status.blade.php</files>
  <action>Extend ReviewRunRepository with eager-loaded read methods for recent runs and detail loads, such as recentWithPullRequestRepository(int $limit = 25) ordered newest first and findWithPullRequestRepositoryOrFail(int|string $id). Update ReviewController@index to use the recent read method and pass the runs to the dashboard. Update ReviewController@show to use the repository detail method and return resources/views/reviews/show.blade.php; the controller must not perform Eloquent queries directly per ARCH-02 and ARCH-04. Complete resources/views/reviews/index.blade.php history rendering per 01-UI-SPEC: empty state when no runs; otherwise a scan-friendly table or stacked row list with status near the left, repository full name, PR number, source URL, created timestamp, safe failed summary when status is failed, and one detail link with copy View review run. Expand the minimal resources/views/reviews/show.blade.php shell from Plan 01-03 into the completed detail shell with required content order: Back to review runs, page title Review Run #{id}, status summary band, pull request identity section, run metadata section, and failure section only when status is failed per D-06. For failed runs, show Review run failed, render only safe_error_message or the fallback "The run failed, but no safe error summary was recorded.", and include the exact next-step sentence "Review the safe error summary, then create a new run after fixing the source issue." Show source_url as a safe external link; omit missing lifecycle timestamps; never show GitHub file lists, diff hunks, commits, findings, drafts, queue controls, retry/cancel/delete actions, or provider setup. Update the status component so pending uses teal, queued/running/cancelled use warning or neutral, completed uses green, failed uses red, all labels are title case, and focus/link styles remain visible.</action>
  <verify>
    <automated>php artisan test --testsuite=Feature --filter=ReviewRunHistoryAndDetailTest</automated>
    <automated>php artisan test --testsuite=Feature</automated>
  </verify>
  <acceptance_criteria>The dashboard history and detail shell render persisted review run identity/status/failure data through repository-backed reads, the failed detail section includes the required next-step sentence, and no raw unsafe error text or out-of-scope review analysis UI appears.</acceptance_criteria>
  <done>The repository-backed dashboard history, detail shell, failure section, and status component pass the focused history/detail tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 1 validation and UI contract audit</name>
  <read_first>.planning/phases/01-review-run-foundation-and-management-ui/01-UI-SPEC.md, .planning/phases/01-review-run-foundation-and-management-ui/01-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <files>app/Http/Controllers/ReviewController.php, app/Repositories/ReviewRunRepository.php, resources/views/reviews/index.blade.php, resources/views/reviews/show.blade.php, resources/views/components/review-status.blade.php, tests/Feature/ReviewRunHistoryAndDetailTest.php</files>
  <action>Run the full suite and inspect UI files against 01-UI-SPEC. Confirm the first screen is still /reviews, the header says Laravel AI PR Review, the page title is Review Runs, the dashboard order remains create form before Recent Review Runs, detail order matches the contract, status labels are Pending, Queued, Running, Completed, Failed, Cancelled, failure text uses only safe_error_message or the required fallback, and the failed detail section includes the exact next-step sentence "Review the safe error summary, then create a new run after fixing the source issue." Confirm long URLs/repository names wrap or truncate without horizontal scrolling. Confirm no landing hero, nested cards, gradients, decorative imagery, auth prompts, GitHub token setup, provider selection, queue controls, findings, drafts, approval, publishing, webhook settings, delete, cancel, retry, or bulk actions were introduced. Confirm Phase 1 requirement IDs RUN-01 through RUN-07, ARCH-01 through ARCH-04, and GH-01 are covered by tests across the four plans.</action>
  <verify>
    <automated>php artisan test --testsuite=Feature</automated>
    <automated>composer run test</automated>
  </verify>
  <acceptance_criteria>Full suite passes and the Phase 1 vertical slice is user-visible end to end: submit a GitHub PR URL, create a pending run, view it in history, and open its detail shell with safe status/failure presentation and required failed next-step copy.</acceptance_criteria>
  <done>The full Phase 1 slice passes automated verification and UI contract audit.</done>
</task>

</tasks>

## Artifacts this phase produces

- Completed history rendering in `resources/views/reviews/index.blade.php`
- Completed detail shell in `resources/views/reviews/show.blade.php`
- Completed status pill behavior in `resources/views/components/review-status.blade.php`
- Eager-loaded history/detail read methods in `App\Repositories\ReviewRunRepository`
- History/detail/failure feature tests in `tests/Feature/ReviewRunHistoryAndDetailTest.php`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database records to Blade views | Persisted source URLs and safe error summaries are rendered back to the browser. |
| Route parameter to repository lookup | Untrusted /reviews/{id} parameter is resolved through repository lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | resources/views/reviews/show.blade.php | medium | mitigate | Render only safe_error_message or required safe fallback for failed runs and never raw exceptions, stack traces, credentials, provider payloads, or invalid submitted URL attempts. |
| T-01-04-02 | Tampering | ReviewController@show | low | mitigate | Load detail records through ReviewRunRepository and Laravel escaping; no update or destructive action exists in Phase 1. |
| T-01-04-03 | Denial of Service | Dashboard history | low | mitigate | Limit recent run query to a small operational count such as 25 and wrap/truncate long URLs to avoid layout breakage. |
</threat_model>

<verification>
Run `php artisan test --testsuite=Feature --filter=ReviewRunHistoryAndDetailTest`, then `php artisan test --testsuite=Feature`, then `composer run test`. Verify that UI contract coverage remains automated and that manual inspection finds no copy implying GitHub fetching, AI review, findings, drafts, or publishing.
</verification>

<success_criteria>
The Phase 1 vertical slice is complete when `/reviews` shows persisted review run history, each row links to `/reviews/{id}`, the detail shell shows run identity/status/timestamps/source URL, failed runs show safe summarized errors with the required next-step sentence, and all reserved statuses render predictably.
</success_criteria>

<output>
Create `.planning/phases/01-review-run-foundation-and-management-ui/01-04-SUMMARY.md` when done.
</output>
