---
phase: 04-draft-review-and-custom-instructions
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - app/Http/Controllers/ReviewController.php
  - app/Http/Controllers/ReviewInstructionSettingController.php
  - app/Models/ReviewInstructionSetting.php
  - app/Repositories/ReviewInstructionSettingRepository.php
  - app/Services/ReviewInstructionSettingService.php
  - database/migrations/2026_06_28_100200_create_review_instruction_settings_table.php
  - resources/views/reviews/show.blade.php
  - routes/web.php
  - tests/Feature/CustomReviewInstructionsPersistenceTest.php
  - tests/Feature/CustomReviewInstructionsTest.php
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-04
must_haves:
  truths:
    - "Per D-13 and RULE-01, the management interface shows one global `Custom Review Instructions` textarea, not multiple rule sets or profiles."
    - "Per D-14, D-15, and RULE-04, custom instructions persist in storage separate from findings and drafts, and saving them does not rewrite existing generated artifacts."
    - "Per RULE-02, the settings form validates textarea input and keeps its errors isolated from the draft workflow on the same detail page."
  artifacts:
    - app/Http/Controllers/ReviewController.php
    - app/Http/Controllers/ReviewInstructionSettingController.php
    - app/Models/ReviewInstructionSetting.php
    - app/Repositories/ReviewInstructionSettingRepository.php
    - app/Services/ReviewInstructionSettingService.php
    - database/migrations/2026_06_28_100200_create_review_instruction_settings_table.php
    - resources/views/reviews/show.blade.php
    - routes/web.php
    - tests/Feature/CustomReviewInstructionsPersistenceTest.php
    - tests/Feature/CustomReviewInstructionsTest.php
  key_links:
    - "`ReviewInstructionSettingRepository` must own the singleton global row so mutable settings never live on findings or drafts per D-14."
    - "`ReviewController::show()` and `resources/views/reviews/show.blade.php` must surface the current saved value without interfering with draft-form validation per RULE-02."
    - "`ReviewInstructionSettingService` must normalize empty input to a stable stored shape that future execution integration can consume deterministically."
  prohibitions:
    - "This plan must not yet change `ReviewInstructionBuilder` or `ReviewExecutionService`; future-request integration belongs to 04-05."
---

## Phase Goal

**As a** reviewer tuning future runs, **I want to** view and edit one global set of custom instructions in the management UI, **so that** I can change future review guidance without mutating historical findings or drafts.

<objective>
Add the custom-instructions management slice: singleton storage, validation, and detail-page UI per D-13 through D-15 and RULE-01, RULE-02, RULE-04.

Purpose: make instruction settings user-editable before wiring them into future execution.
Output: settings table/model/repository/service/controller, detail-page textarea UI, and feature coverage for separate persistence plus update behavior.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-CONTEXT.md
@.planning/phases/04-draft-review-and-custom-instructions/04-RESEARCH.md
@.planning/phases/04-draft-review-and-custom-instructions/04-PATTERNS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-03-PLAN.md
@app/Http/Controllers/ReviewController.php
@resources/views/reviews/show.blade.php
@routes/web.php
</context>

<tasks>

<task type="auto">
  <name>Task 04-04-01: Lock global custom-instructions storage and UI in RED tests</name>
  <files>tests/Feature/CustomReviewInstructionsTest.php, tests/Feature/CustomReviewInstructionsPersistenceTest.php</files>
  <action>Create failing feature coverage for the settings slice. In `tests/Feature/CustomReviewInstructionsTest.php`, assert the review detail page renders a `Custom Review Instructions` section with the current stored value per D-13 and RULE-01, and that `PUT /review-instructions` named `review-instructions.update` validates and saves textarea input using a named error bag that does not conflict with draft forms per RULE-02. In `tests/Feature/CustomReviewInstructionsPersistenceTest.php`, assert the saved text lives in a separate table and that saving instructions does not rewrite existing findings, drafts, statuses, or stale markers per D-14 and D-15.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='CustomReviewInstructionsTest|CustomReviewInstructionsPersistenceTest'</automated>
  </verify>
  <done>The settings slice fails under test before persistence and Blade changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-04-02: Implement singleton settings persistence and management UI</name>
  <files>app/Http/Controllers/ReviewController.php, app/Http/Controllers/ReviewInstructionSettingController.php, app/Models/ReviewInstructionSetting.php, app/Repositories/ReviewInstructionSettingRepository.php, app/Services/ReviewInstructionSettingService.php, database/migrations/2026_06_28_100200_create_review_instruction_settings_table.php, resources/views/reviews/show.blade.php, routes/web.php, tests/Feature/CustomReviewInstructionsPersistenceTest.php, tests/Feature/CustomReviewInstructionsTest.php</files>
  <behavior>
    - The detail page shows one global custom-instructions textarea with the current saved value.
    - Saving the form updates one singleton row stored separately from findings and drafts.
    - Validation errors for the settings form stay isolated from draft workflow errors.
  </behavior>
  <action>Create `database/migrations/2026_06_28_100200_create_review_instruction_settings_table.php` with unique `scope`, nullable `custom_instructions`, and timestamps. Implement `ReviewInstructionSetting`, `ReviewInstructionSettingRepository`, and `ReviewInstructionSettingService` with `findGlobal()`, `getOrCreateGlobal()`, and `updateGlobal()` behavior that preserves D-13 and D-14. Add `ReviewInstructionSettingController::update()` and route `PUT /review-instructions` named `review-instructions.update`; validate with bag `instructions` and normalize empty text to `null`. Update `ReviewController::show()` to pass the current global settings value to the view, then extend `resources/views/reviews/show.blade.php` with a clearly separate custom-instructions section below the draft workflow. Keep this a single global textarea only.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='CustomReviewInstructionsTest|CustomReviewInstructionsPersistenceTest'</automated>
  </verify>
  <done>The user can now inspect and update one global custom-instructions record without affecting generated artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered instruction text → persisted settings row | Arbitrary reviewer text becomes reusable application state here. |
| settings storage → detail-page rendering | Mutable configuration becomes visible alongside draft workflow here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | global instruction settings | medium | mitigate | Restrict writes to one repository-owned singleton row and validate textarea length/content before persistence. |
| T-04-09 | Information Disclosure | settings persistence boundary | medium | mitigate | Store custom instructions only in the dedicated table; never duplicate them onto findings, drafts, or secret-bearing config. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='CustomReviewInstructionsTest|CustomReviewInstructionsPersistenceTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Http/Controllers/ReviewController.php app/Http/Controllers/ReviewInstructionSettingController.php app/Models/ReviewInstructionSetting.php app/Repositories/ReviewInstructionSettingRepository.php app/Services/ReviewInstructionSettingService.php routes/web.php tests/Feature/CustomReviewInstructionsTest.php tests/Feature/CustomReviewInstructionsPersistenceTest.php`
</verification>

<success_criteria>
- The detail page shows one global custom-instructions textarea with the current value.
- Saving the form updates only the dedicated settings record.
- Draft and findings persistence remain unchanged when settings are edited.
</success_criteria>

<output>
Create `.planning/phases/04-draft-review-and-custom-instructions/04-04-SUMMARY.md` when done
</output>
