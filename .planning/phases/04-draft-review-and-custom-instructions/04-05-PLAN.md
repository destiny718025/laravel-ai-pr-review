---
phase: 04-draft-review-and-custom-instructions
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - app/Repositories/ReviewInstructionSettingRepository.php
  - app/Services/AI/ReviewInstructionBuilder.php
  - app/Services/ReviewExecutionService.php
  - tests/Feature/QueuedReviewExecutionTest.php
  - tests/Unit/AI/ReviewInstructionBuilderTest.php
autonomous: true
requirements:
  - RULE-03
  - RULE-04
must_haves:
  truths:
    - "Per D-16, D-17, and RULE-03, future AI review requests and retries load the latest saved global custom instructions and compose them with the default instruction block deterministically."
    - "Per D-15 and RULE-04, changing the saved instructions alters future request composition only; it does not rewrite historical findings, drafts, or stale markers."
    - "The AI provider abstraction remains unchanged: only the instruction-building input changes, not the provider boundary itself."
  artifacts:
    - app/Repositories/ReviewInstructionSettingRepository.php
    - app/Services/AI/ReviewInstructionBuilder.php
    - app/Services/ReviewExecutionService.php
    - tests/Feature/QueuedReviewExecutionTest.php
    - tests/Unit/AI/ReviewInstructionBuilderTest.php
  key_links:
    - "`ReviewExecutionService` must load the latest global settings at request-build time so retries use fresh instructions per D-16."
    - "`ReviewInstructionBuilder` must preserve the default guidance and append saved custom text in a deterministic block per D-17."
    - "Execution integration must not snapshot custom instructions onto `review_runs` in this phase because D-15 and RULE-04 keep settings separate from generated artifacts."
  prohibitions:
    - "This plan must not add multi-profile rule storage, per-repository overrides, or UI changes beyond what 04-04 already established."
---

## Phase Goal

**As a** reviewer updating guidance over time, **I want to** have future runs and retries automatically include the latest saved custom instructions, **so that** the AI review workflow follows my current preferences without retroactively changing prior results.

<objective>
Finish Phase 04 by integrating saved instruction settings into future AI request construction per D-15, D-16, D-17 and RULE-03, RULE-04.

Purpose: connect the settings slice to execution without collapsing mutable configuration into historical review data.
Output: deterministic builder composition, execution-service integration, and unit/feature coverage for future-request behavior.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-CONTEXT.md
@.planning/phases/04-draft-review-and-custom-instructions/04-RESEARCH.md
@.planning/phases/04-draft-review-and-custom-instructions/04-PATTERNS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-04-PLAN.md
@app/Services/AI/ReviewInstructionBuilder.php
@app/Services/ReviewExecutionService.php
</context>

<tasks>

<task type="auto">
  <name>Task 04-05-01: Lock future-request instruction composition in RED tests</name>
  <files>tests/Feature/QueuedReviewExecutionTest.php, tests/Unit/AI/ReviewInstructionBuilderTest.php</files>
  <action>Create failing coverage for the final slice. In `tests/Unit/AI/ReviewInstructionBuilderTest.php`, assert deterministic composition: default instruction lines stay first, blank custom text yields default-only output, and non-empty saved custom text is appended in one stable section per D-17. In `tests/Feature/QueuedReviewExecutionTest.php`, assert that after saving custom instructions the next AI execution request and the next retry both include the saved text, while existing findings, drafts, and stale markers remain unchanged per D-15 and D-16.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewInstructionBuilderTest|QueuedReviewExecutionTest'</automated>
  </verify>
  <done>The future-request contract fails under test before instruction-builder and execution wiring changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-05-02: Compose saved instructions into future execution and retry requests</name>
  <files>app/Repositories/ReviewInstructionSettingRepository.php, app/Services/AI/ReviewInstructionBuilder.php, app/Services/ReviewExecutionService.php, tests/Feature/QueuedReviewExecutionTest.php, tests/Unit/AI/ReviewInstructionBuilderTest.php</files>
  <behavior>
    - Blank saved instructions leave default request instructions unchanged.
    - Non-empty saved instructions are appended deterministically to the default block.
    - New executions and retries both load the latest global settings before building the provider request.
  </behavior>
  <action>Refactor `ReviewInstructionBuilder` to expose a deterministic composition method such as `buildWithCustomInstructions(?string $customInstructions): string` that preserves the existing default guidance and appends a clearly delimited custom-instructions block only when the saved text is non-empty. Update `ReviewExecutionService` to load the latest global setting through `ReviewInstructionSettingRepository` at request-build time and pass that text into the builder for both new runs and retries. Keep the provider interface and request DTO unchanged beyond the composed instructions string, and do not snapshot custom instructions on `review_runs` or mutate existing draft/finding records in this plan.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewInstructionBuilderTest|QueuedReviewExecutionTest'</automated>
  </verify>
  <done>Future executions and retries use the latest saved custom instructions through deterministic builder composition, completing Phase 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings → AI request instructions | Mutable user guidance crosses into provider-request construction here. |
| retry trigger → request rebuild | A retried run must reload current guidance rather than reuse stale instructions here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Integrity | instruction composition | high | mitigate | Keep builder composition deterministic and covered by unit tests so saved text cannot reorder or erase default safety guidance unexpectedly. |
| T-04-11 | Tampering | retry request construction | medium | mitigate | Load global settings at request-build time for every execution and retry so stale cached instructions are not reused. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewInstructionBuilderTest|QueuedReviewExecutionTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Services/AI/ReviewInstructionBuilder.php app/Services/ReviewExecutionService.php tests/Unit/AI/ReviewInstructionBuilderTest.php tests/Feature/QueuedReviewExecutionTest.php`
</verification>

<success_criteria>
- Saving custom instructions affects the next execution and retry request payloads.
- Default instruction guidance remains intact when custom instructions are blank.
- Historical findings, drafts, and stale state remain unchanged when settings are edited.
</success_criteria>

<output>
Create `.planning/phases/04-draft-review-and-custom-instructions/04-05-SUMMARY.md` when done
</output>
