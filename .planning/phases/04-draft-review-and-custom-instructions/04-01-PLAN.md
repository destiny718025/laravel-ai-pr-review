---
phase: 04-draft-review-and-custom-instructions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/ReviewCommentDraftStatus.php
  - app/Models/ReviewCommentDraft.php
  - app/Models/ReviewFinding.php
  - app/Models/ReviewRun.php
  - app/Repositories/ReviewCommentDraftRepository.php
  - app/Repositories/ReviewFindingRepository.php
  - database/factories/ReviewCommentDraftFactory.php
  - database/factories/ReviewFindingFactory.php
  - database/migrations/2026_06_28_100000_add_superseded_at_to_review_findings_table.php
  - database/migrations/2026_06_28_100100_create_review_comment_drafts_table.php
  - tests/Feature/QueuedReviewExecutionTest.php
  - tests/Feature/ReviewDraftPersistenceFoundationTest.php
autonomous: true
requirements:
  - DRAFT-01
  - DRAFT-06
  - DRAFT-07
must_haves:
  truths:
    - "Per D-04, D-05, D-09, D-22, and D-23, draft records exist independently from findings, keep source-finding linkage, retain currently available GitHub targeting metadata, and start in explicit workflow states that Phase 5 can extend."
    - "Per D-18 and D-19, successful retries can preserve draft provenance because findings become current-versus-superseded records instead of being physically deleted."
    - "Per DRAFT-01, the app still has a concrete way to load the active findings for a completed review run after provenance-safe schema changes land."
  artifacts:
    - app/Enums/ReviewCommentDraftStatus.php
    - app/Models/ReviewCommentDraft.php
    - app/Models/ReviewFinding.php
    - app/Models/ReviewRun.php
    - app/Repositories/ReviewCommentDraftRepository.php
    - app/Repositories/ReviewFindingRepository.php
    - database/migrations/2026_06_28_100000_add_superseded_at_to_review_findings_table.php
    - database/migrations/2026_06_28_100100_create_review_comment_drafts_table.php
    - tests/Feature/ReviewDraftPersistenceFoundationTest.php
    - tests/Feature/QueuedReviewExecutionTest.php
  key_links:
    - "`ReviewFindingRepository` must expose explicit supersede/store-current seams so later retry logic can satisfy D-18 and D-19 without breaking D-04 provenance."
    - "`ReviewRun` and `ReviewFinding` relationships must distinguish current findings from superseded findings while keeping `ReviewCommentDraft.source_review_finding_id` valid per D-04."
    - "`review_comment_drafts` must carry copied targeting metadata and `stale_at` so later generation, stale warnings, and publication wiring do not need a corrective migration."
  prohibitions:
    - "Per D-03, D-07, and deferred Phase 5 scope, this plan must not call any AI provider, create draft-generation routes, or add GitHub publication behavior."
---

## Phase Goal

**As a** reviewer depending on stable review history, **I want to** preserve finding and draft provenance before draft UI actions are added, **so that** later Phase 4 slices can build on durable records instead of brittle retry behavior.

<objective>
Establish the Phase 04 persistence foundation: draft records, explicit status vocabulary, and superseded-finding provenance per D-04, D-05, D-09, D-18, D-19, D-22, and D-23.

Purpose: remove the hard-delete retry hazard before any user-facing draft workflow depends on it.
Output: `review_comment_drafts` schema, current-versus-superseded finding semantics, factories, repository seams, and failing-then-green persistence coverage.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-CONTEXT.md
@.planning/phases/04-draft-review-and-custom-instructions/04-RESEARCH.md
@.planning/phases/04-draft-review-and-custom-instructions/04-PATTERNS.md
@.planning/phases/03-queued-ai-review-and-structured-findings/03-05-SUMMARY.md
@app/Models/ReviewRun.php
@app/Models/ReviewFinding.php
@app/Repositories/ReviewFindingRepository.php
</context>

<tasks>

<task type="auto">
  <name>Task 04-01-01: Lock provenance-safe persistence in RED tests</name>
  <files>tests/Feature/ReviewDraftPersistenceFoundationTest.php, tests/Feature/QueuedReviewExecutionTest.php, database/factories/ReviewFindingFactory.php</files>
  <action>Create failing persistence-focused coverage for the first slice. In `tests/Feature/ReviewDraftPersistenceFoundationTest.php`, assert a `ReviewRun` can load `currentFindings`, superseded findings, and persisted `drafts`, and that each draft keeps `source_review_finding_id`, `file_path`, nullable `line_reference`, `github_head_sha`, nullable `source_file_sha`, `status`, and nullable `stale_at` per D-04, D-05, D-09, D-22, and D-23. Extend `tests/Feature/QueuedReviewExecutionTest.php` with a red assertion that successful retry no longer relies on physical finding deletion because D-18 and D-19 require provenance-safe replacement. Update `database/factories/ReviewFindingFactory.php` only as needed so superseded/current setup is easy to express in tests.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewDraftPersistenceFoundationTest|QueuedReviewExecutionTest'</automated>
  </verify>
  <done>The persistence contract fails for the right reasons before migrations, relations, and repositories are changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-01-02: Add draft schema and superseded-finding repository seams</name>
  <files>app/Enums/ReviewCommentDraftStatus.php, app/Models/ReviewCommentDraft.php, app/Models/ReviewFinding.php, app/Models/ReviewRun.php, app/Repositories/ReviewCommentDraftRepository.php, app/Repositories/ReviewFindingRepository.php, database/factories/ReviewCommentDraftFactory.php, database/factories/ReviewFindingFactory.php, database/migrations/2026_06_28_100000_add_superseded_at_to_review_findings_table.php, database/migrations/2026_06_28_100100_create_review_comment_drafts_table.php, tests/Feature/QueuedReviewExecutionTest.php, tests/Feature/ReviewDraftPersistenceFoundationTest.php</files>
  <behavior>
    - Draft rows belong to a review run and a source finding, start in `draft`, and retain copied targeting metadata.
    - Findings can be queried as current or superseded without invalidating draft provenance.
    - Repository APIs exist for later generation and retry logic without embedding workflow rules in controllers.
  </behavior>
  <action>Create `App\Enums\ReviewCommentDraftStatus` with `draft`, `approved`, `posted`, and `failed` states per D-05 and D-09. Add `database/migrations/2026_06_28_100100_create_review_comment_drafts_table.php` with `review_run_id`, `source_review_finding_id`, `status`, `body`, `file_path`, nullable `line_reference`, `github_head_sha`, nullable `source_file_sha`, nullable `stale_at`, and timestamps. Add `database/migrations/2026_06_28_100000_add_superseded_at_to_review_findings_table.php` with nullable `superseded_at`. Implement `App\Models\ReviewCommentDraft`, `ReviewRun::drafts()`, `ReviewRun::currentFindings()`, `ReviewFinding::sourceDrafts()`, and casts/relations needed for DRAFT-01 and DRAFT-07. Refactor `ReviewFindingRepository` so later execution code can call explicit methods such as `supersedeCurrentForReviewRun()` and `storeCurrentForReviewRun()` instead of hard-deleting rows. Add `ReviewCommentDraftRepository` methods for creating draft rows, loading run drafts with source findings, and marking rows stale later, but do not add generation or approval workflow here. Back the new tests with `ReviewCommentDraftFactory` and any minimal factory updates required for current versus superseded findings.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewDraftPersistenceFoundationTest|QueuedReviewExecutionTest'</automated>
  </verify>
  <done>Phase 04 has durable draft and finding primitives that later plans can use without violating provenance or status requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted findings → persisted drafts | Existing AI output becomes publication-candidate records here. |
| retry success path → historical provenance | A fresh finding set can otherwise invalidate local draft history here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | draft/finding provenance foundation | high | mitigate | Use `superseded_at` and durable source-finding foreign keys so retries preserve traceability instead of deleting the records draft workflows depend on. |
| T-04-02 | Information Disclosure | copied targeting metadata | medium | mitigate | Persist only the already-available publication metadata required by D-22 and D-23; do not add provider payloads, tokens, or write secrets to draft rows. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewDraftPersistenceFoundationTest|QueuedReviewExecutionTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Enums/ReviewCommentDraftStatus.php app/Models/ReviewCommentDraft.php app/Models/ReviewFinding.php app/Models/ReviewRun.php app/Repositories/ReviewCommentDraftRepository.php app/Repositories/ReviewFindingRepository.php tests/Feature/ReviewDraftPersistenceFoundationTest.php`
</verification>

<success_criteria>
- Draft persistence exists independently from findings and is ready for status transitions.
- Current and superseded findings can coexist without breaking source-finding traceability.
- Later retry and generation plans no longer depend on hard-delete finding replacement.
</success_criteria>

<output>
Create `.planning/phases/04-draft-review-and-custom-instructions/04-01-SUMMARY.md` when done
</output>
