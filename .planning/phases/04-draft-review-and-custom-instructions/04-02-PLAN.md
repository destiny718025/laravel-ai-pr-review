---
phase: 04-draft-review-and-custom-instructions
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/Http/Controllers/ReviewDraftController.php
  - app/Repositories/ReviewCommentDraftRepository.php
  - app/Repositories/ReviewFindingRepository.php
  - app/Repositories/ReviewRunRepository.php
  - app/Services/ReviewDraftService.php
  - resources/views/reviews/show.blade.php
  - routes/web.php
  - tests/Feature/ReviewDraftGenerationTest.php
  - tests/Feature/ReviewDraftMetadataTest.php
  - tests/Feature/ReviewDraftPresentationTest.php
autonomous: true
requirements:
  - DRAFT-02
  - DRAFT-03
  - DRAFT-07
must_haves:
  truths:
    - "Per D-01, D-02, D-03, and D-12, the review detail page exposes a manual `Generate Drafts` action that creates missing drafts only from persisted `suggested_comment_text`."
    - "Per D-10 and D-11, the detail page shows `Structured Findings` as read-only output and `Comment Drafts` as a separate operational section."
    - "Per D-04, D-22, D-23, and DRAFT-07, generated drafts preserve source-finding linkage and currently available publication metadata."
  artifacts:
    - app/Http/Controllers/ReviewDraftController.php
    - app/Repositories/ReviewRunRepository.php
    - app/Services/ReviewDraftService.php
    - resources/views/reviews/show.blade.php
    - routes/web.php
    - tests/Feature/ReviewDraftGenerationTest.php
    - tests/Feature/ReviewDraftMetadataTest.php
    - tests/Feature/ReviewDraftPresentationTest.php
  key_links:
    - "`ReviewDraftService::generateMissingDraftsForReviewRun()` must query current findings without drafts and persist copied body plus metadata transactionally per D-02 and D-03."
    - "`ReviewRunRepository` and `resources/views/reviews/show.blade.php` must render findings and drafts as separate read models per D-10 through D-12."
    - "The generation route must remain local-only with no AI regeneration or GitHub write path per D-03 and deferred Phase 5 scope."
  prohibitions:
    - "This plan must not add draft editing, approval, stale-marking retry behavior, or custom-instructions persistence; those belong to later plans."
---

## Phase Goal

**As a** reviewer inspecting a completed run, **I want to** generate comment drafts from existing findings and see them beside the read-only findings, **so that** I can start curating publication candidates without leaving the detail page.

<objective>
Deliver the first user-visible Phase 04 slice: manual draft generation and split findings-versus-drafts presentation per D-01 through D-04 and D-10 through D-12.

Purpose: turn the persistence foundation into a visible workflow that still stays safely inside the local application boundary.
Output: `Generate Drafts` route/controller/service wiring, detail-page presentation, and generation/presentation/metadata tests.
</objective>

<execution_context>
@/Users/tang/.codex/gsd-core/workflows/execute-plan.md
@/Users/tang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-CONTEXT.md
@.planning/phases/04-draft-review-and-custom-instructions/04-RESEARCH.md
@.planning/phases/04-draft-review-and-custom-instructions/04-PATTERNS.md
@.planning/phases/04-draft-review-and-custom-instructions/04-01-PLAN.md
@app/Http/Controllers/ReviewController.php
@app/Repositories/ReviewRunRepository.php
@resources/views/reviews/show.blade.php
@routes/web.php
</context>

<tasks>

<task type="auto">
  <name>Task 04-02-01: Lock manual generation and split presentation in RED tests</name>
  <files>tests/Feature/ReviewDraftPresentationTest.php, tests/Feature/ReviewDraftGenerationTest.php, tests/Feature/ReviewDraftMetadataTest.php</files>
  <action>Create failing feature coverage for the manual generation slice. In `tests/Feature/ReviewDraftPresentationTest.php`, assert the detail page renders separate `Structured Findings` and `Comment Drafts` sections, keeps findings read-only per D-10 and D-11, and shows a visible `Generate Drafts` action per D-01. In `tests/Feature/ReviewDraftGenerationTest.php`, add failing coverage for `POST /reviews/{reviewRun}/drafts/generate` named `reviews.drafts.generate` that creates drafts only for current findings without an existing draft per D-02 and copies `suggested_comment_text` verbatim into the draft body per D-03. In `tests/Feature/ReviewDraftMetadataTest.php`, assert generated drafts keep `source_review_finding_id`, `file_path`, nullable `line_reference`, `github_head_sha`, and nullable `source_file_sha` per D-04, D-22, and D-23. Include negative assertions that this slice never posts to GitHub or re-calls the AI provider.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewDraftPresentationTest|ReviewDraftGenerationTest|ReviewDraftMetadataTest'</automated>
  </verify>
  <done>The first user-facing draft workflow is encoded as failing tests before route, service, and Blade changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02-02: Implement manual missing-only generation and separate detail-page sections</name>
  <files>app/Http/Controllers/ReviewDraftController.php, app/Repositories/ReviewCommentDraftRepository.php, app/Repositories/ReviewFindingRepository.php, app/Repositories/ReviewRunRepository.php, app/Services/ReviewDraftService.php, resources/views/reviews/show.blade.php, routes/web.php, tests/Feature/ReviewDraftGenerationTest.php, tests/Feature/ReviewDraftMetadataTest.php, tests/Feature/ReviewDraftPresentationTest.php</files>
  <behavior>
    - Clicking `Generate Drafts` creates only missing drafts for current findings in one review run.
    - The detail page shows read-only findings separately from operational drafts.
    - Generated draft bodies come from persisted `suggested_comment_text`, not from new provider output.
  </behavior>
  <action>Add `ReviewDraftController::generate()` plus route `POST /reviews/{reviewRun}/drafts/generate` named `reviews.drafts.generate`. Implement `ReviewDraftService::generateMissingDraftsForReviewRun()` so it loads the run via `ReviewRunRepository`, asks `ReviewFindingRepository` for current findings that have no draft, maps each finding to a draft row whose `body` is the finding's `suggested_comment_text`, and persists rows through `ReviewCommentDraftRepository` inside a transaction. Update `ReviewRunRepository` to eager-load `currentFindings`, `drafts`, `drafts.sourceFinding`, `files`, and `pullRequest.repository` for the detail page. Rewrite `resources/views/reviews/show.blade.php` so `Structured Findings` remains read-only, `Comment Drafts` is a distinct section with generate button and empty-state copy, and there are no edit or approval controls yet.</action>
  <verify>
    <automated>docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewDraftPresentationTest|ReviewDraftGenerationTest|ReviewDraftMetadataTest'</automated>
  </verify>
  <done>A completed run can now generate missing drafts locally and show findings versus drafts as separate concepts on the same page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted findings → generated drafts | Existing AI text is promoted into editable publication candidates here. |
| repository detail aggregate → Blade detail page | Source metadata and draft text become user-visible here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | manual draft generation | high | mitigate | Generate drafts only for current findings without an existing draft and do the write transactionally so D-02 idempotency is enforced server-side. |
| T-04-04 | Elevation of Privilege | detail-page draft actions | high | mitigate | Keep the slice local-only: no provider regeneration and no GitHub publication routes or services are permitted in this plan. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation is part of this plan. |
</threat_model>

<verification>
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 php artisan test --filter='ReviewDraftPresentationTest|ReviewDraftGenerationTest|ReviewDraftMetadataTest'`
- `docker exec -w /var/www/laravel-ai-pr-review laradock-workspace-85-1 ./vendor/bin/pint app/Http/Controllers/ReviewDraftController.php app/Repositories/ReviewRunRepository.php app/Services/ReviewDraftService.php routes/web.php tests/Feature/ReviewDraftPresentationTest.php tests/Feature/ReviewDraftGenerationTest.php tests/Feature/ReviewDraftMetadataTest.php`
</verification>

<success_criteria>
- The detail page exposes `Generate Drafts` and keeps findings read-only in a separate section.
- Clicking the action creates one draft per current finding without an existing draft.
- Generated drafts preserve source linkage and targeting metadata needed for later publication.
</success_criteria>

<output>
Create `.planning/phases/04-draft-review-and-custom-instructions/04-02-SUMMARY.md` when done
</output>
